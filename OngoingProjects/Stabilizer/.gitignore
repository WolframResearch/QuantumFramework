# Local-only — large LaTeX sources of the 28 papers (re-fetchable via paper-fetch-report.md).
tex/
eprint/

# External package source code (embedded git repositories — could be added as submodules later if needed).
External Packages/

# Mac noise.
.DS_Store
