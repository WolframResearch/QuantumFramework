# Open-System Simulation Catalog — Audit Ledger

Artifact: `open-system-simulation-catalog.md`

Audit scope: the complete Markdown source, including prose, displayed and inline mathematics,
all 304 Wolfram Language fences, all twenty worked examples, and the generated notebook twin.
The audit distinguishes exact mathematical identities, continuum-limit claims, finite-step
algorithms, Monte Carlo evidence, and presentation/serialization defects.

## Resolved findings

| ID | Severity | Finding | Resolution and closure evidence |
|---|---|---|---|
| F01 | Critical | `steadyState` treated a null-space basis as physical steady states. For a zero generator it duplicated one Pauli direction, omitted another, had rank three instead of four, and returned indefinite matrices. | Added `stationaryHermitianBasis` for the complete real Hermitian kernel. `steadyState` now returns a density operator only for a unique stationary direction and otherwise returns `Failure["NonUniqueSteadyState", …]`. Kernel tests cover the four-dimensional zero-generator kernel, two-dimensional dephasing kernel, nonunique failure, and unique amplitude-damping state. |
| F02 | Critical | The QPC eigenmode spectrum was evaluated at the exceptional point even though its eigenbasis is singular; exact evaluation was rank deficient and `LinearSolve` failed. | `qpcSpectrum` now uses the resolvent representation at and away from the exceptional point. `regressionSpectrum` rejects defective or ill-conditioned eigenbases. The exact exceptional-point spectrum is tested finite. |
| F03 | High | The positivity-preserving finite-step measurement update was described as exact Lindblad ensemble evolution, leaving only sampling error. Direct Gaussian quadrature showed a nonzero time-step bias. | Rewrote the claim to separate complete positivity/normalization of an individual step from weak finite-step accuracy. Added deterministic quadrature evidence: biases `0.00493820`, `0.00141839`, and `0.000385336` for steps `0.1`, `0.05`, and `0.025`; the refinement contraction is asserted. |
| F04 | High | Rapid-purification trajectories at finite step were called deterministic and said to hug the continuum exponential path on every run. Reproduction showed substantial path spread and mean bias. | Restricted pathwise determinism to the continuum idealization, parameterized the discrete step, and added a refinement study. The standard deviation contracts from `0.02995` to `0.01476`; the mean bias contracts from `0.009825` to `0.003216`. |
| F05 | High | The shared `expectation` helper discarded the imaginary part for every operator, which is wrong for non-Hermitian operators. | It now returns `Tr[op.rho]`; examples that require a Hermitian real expectation take `Re` locally. A kernel assertion uses a non-Hermitian operator. |
| F06 | High | The density-matrix definition emphasized trace and diagonal entries without stating the decisive positive-semidefinite condition. | The definition now states Hermiticity, positive semidefiniteness, and unit trace; nonnegative diagonal entries are identified as a consequence rather than a substitute. |
| F07 | Medium | `evolve` forced numerical conversion even when the input supported exact or symbolic propagation. | Removed the unconditional `N`, preserving exact-friendly behavior. |
| F08 | High | `measurementStep` silently accepted inconsistent channel dimensions, efficiencies, records, and list lengths, and did not robustly cover empty channel lists. | Added dimension, range, length, time-step, density-operator, record, and normalization checks with structured failures. Kernel tests cover empty lists and malformed efficiencies/lengths. |
| F09 | High | The truncated-oscillator discussion implied the infinite-dimensional commutator held exactly at the cutoff and used off-by-one cutoff language. | `annihilation` is an exact sparse matrix; the finite-dimensional identity `I - n |n-1><n-1|` is derived and tested, and cutoff prose is corrected. |
| F10 | High | The displayed Fourier convention and the spectrum formulas used inconsistent signs. | Standardized the displayed convention to `exp(-i omega t)` and asserted the resolvent implementation follows it. |
| F11 | High | In the QPC section, the coefficient multiplying `D[n1]` was also called the coherence-decay rate, missing the factor of one half. | The prose now distinguishes the dissipator coefficient `kappa` from the off-diagonal decay rate `kappa/2`. |
| F12 | Medium | Modal spectrum terms were described as positive Lorentzians even though complex residues can produce dispersive contributions. | Replaced the universal Lorentzian claim with the correct pole/residue statement and uses the resolvent at singular bases. |
| F13 | Medium | The localization ensemble repeatedly solved the same stochastic trajectory at every plotted time and used parallel evaluation without a demonstrated need. | Each seeded mean trajectory is now solved once and cached; deterministic `Table` replaces incidental parallelism. |
| F14 | Medium | Finite-seed localization evidence was described as a guarantee and a finite-time curve as already asymptotic. | Recast both as empirical finite-window evidence, with the asymptotic distinction made explicit. |
| F15 | High | The Kalman example compared against an estimate initialized outside its own displayed uncertainty band, integrated the Riccati equation too briefly for its asymptotic claim, and overstated endpoint agreement and innovation evidence. | Extended the Riccati interval, disclosed the deliberately inconsistent initial estimate, reports the absolute endpoint discrepancy (about `3e-3`), and calls whiteness a necessary diagnostic rather than proof. The late-time determinant is checked near `1/4`. |
| F16 | High | An initially invalid quantum Brownian covariance was said never to become a valid state, although dissipative evolution can later enter the physical uncertainty region. | Clarified that later validity does not retroactively validate the initial condition and removed the false permanence claim. |
| F17 | High | First-passage simulations substituted the horizon for uncrossed trajectories, silently treating right-censored runs as observed hitting times. | `fptPur` now returns `Missing["RightCensored", …]`; summaries delete missing values and report censor counts. The seeded four-unit comparison has explicit counts `{0,0}`. |
| F18 | High | The linear stochastic Schrödinger norm was called an absolute path probability, and an unweighted ensemble was described without its reference measure. | Identified the squared norm as the Radon–Nikodym/Girsanov likelihood ratio `dP/dQ`, identifies the unweighted result as a `Q`-average, and discloses Euler and sampling error. |
| F19 | Medium | Several finite-cutoff, ODE, thermal, and cooling discrepancies were dismissed as rounding or called exact. | Relabeled them as truncation, discretization, integration, or tolerance errors as appropriate. |
| F20 | Medium | A visualization depended on an external `ResourceFunction`, preventing a self-contained catalog run. | Replaced it with a local `Graphics3D` Bloch-sphere helper. |
| F21 | Medium | The reduced-qubit helper used an opaque manual reshape/sum operation whose subsystem intent was difficult to audit. | Replaced it with an explicit `TensorContract` partial trace. |
| F22 | Low | A cat-state parameter was called a separation although it represented half the coherent-state separation; an axis label inherited the ambiguity. | Renamed the physical description and corrected the label. |
| F23 | Medium | The conclusion stated a property of pure conditioned trajectories without giving the efficiency and monitoring conditions needed for purity preservation. | Scoped the statement to efficient, fully monitored, pure-state conditioning. |
| F24 | Medium | The Markdown contained TeX commands unsupported by the notebook converter (`\Big`, `\gg`, `\operatorname`, `\phantom`, `\underbrace`, and related forms), plus ambiguous row-break tokenization. | Rewrote the formulas with supported equivalents. The converter preflight reports all 73 remaining command names supported. |
| F25 | Medium | The QPC detector record was presented without clearly identifying its centering, rescaling, and sign convention. | The convention is now stated adjacent to the formula. |
| F26 | Medium | A sampled Wigner grid and its interpolant were called exact, conflating exact evaluation of the finite-cutoff formula at grid points with numerical interpolation between them. | The prose now separates pointwise finite-cutoff evaluation from interpolation error. |
| F27 | Medium | A finite-step first-passage sentence still assigned steered runs one deterministic crossing time. | It now states that discrete crossings have a narrow distribution which collapses toward the single continuum-limit time under refinement. |
| F28 | Medium | Notebook serialization split compound TeX indices such as `xx`, `xp`, and `pp` into implicit products with visible spacing, while several plot-label strings embedded active box escapes. | Compound indices are emitted as single roman labels and plot strings use portable plain text. The regenerated notebook passes the structural validator and representative rendered-page inspection. |
| F29 | High | The first independent correctness pass showed that `measurementStep` still accepted nonphysical density matrices, non-Hermitian Hamiltonians, and complex measurement records; complex efficiencies and time steps also emitted ordered-comparison messages. | Added tolerance-aware real-scalar, Hermitian-matrix, and density-operator predicates; all physical checks now precede the update. The no-message regression suite covers bad trace, Hermiticity, positivity, Hamiltonian Hermiticity, records, efficiencies, and time steps. |
| F30 | Medium | The second independent correctness pass found that scalar/empty Hamiltonians could reach `IdentityMatrix` before validation, non-list channel containers remained unevaluated, and three checked-in runners used `Quiet` while claiming to detect messages. | Validation now rejects malformed containers and zero-dimensional Hamiltonians before construction, and rejects non-list records explicitly. Removed message suppression from the full-cell, example, numerical, and notebook-import gates; each gate captures and fails on `$MessageList`. |
| F31 | High | The notebook converter silently dropped or misparsed several vertical-bar and Dirac TeX spellings, leaving notation blank, literalizing commands, or treating a closing bra bar as a division operator. | Kets now use the converter-native `\left|...\right\rangle` form, absolute values use balanced `\left|...\right|` delimiters, outer products use ket times ket-adjoint, and inner products use unambiguous two-argument angle brackets. Removed unsupported `\ket`, `\bra`, and `\braket` macros. The TeX preflight and strict box validator pass; contact sheets cover all 83 rendered pages and high-resolution spot checks cover qubit, Fock, cat, counting, linear-SSE, spectrum, and conclusion pages. |
| F32 | Medium | The converter dropped the argument of a displayed `\exp\![...]` cat-coherence formula; related negative-space controls presented the same serialization risk. | Removed all `\!` controls and used explicit parenthesized exponential arguments. Rebuilt and visually confirmed the exact and short-time coherence law, conditional-cavity exponential, Gaussian exponential, and QPC trace formulas. |
| F33 | High | A provisional Dirac-notation rewrite had corrupted two physics formulas: the normalized photon-counting stochastic Schrödinger equation had a malformed expectation/state factor, and the Mollow elastic weight was written as a ket of a bra. | Re-derived both expressions. The jump equation now has the standard normalized click and no-click increments acting on the state, and the elastic line carries the squared modulus of the steady-state lowering-operator mean. The exhaustive loud run and examples 7 and 20 assertions pass on the corrected source. |
| F34 | High | The front end silently reduced the long four-term cat density-matrix display to `rho(t)=p+c`, although the Markdown equation was complete. | Defined the rank-one operator `P(u,v)` once and rewrote the display with round-grouped `P` terms. The stored box tree contains all four terms and rendered PDF page 24 visibly shows the complete equation. |
| F35 | Medium | The notebook build and render helpers did not fail on emitted messages, and the renderer required an undocumented external `UsingFrontEnd` wrapper in a fresh kernel. | Both helpers now capture `$MessageList` and return a nonzero exit status on any message. The renderer attaches the front end internally; a direct fresh `-noinit -script` run opens the notebook and exports the 83-page PDF with no messages. |

## Verification gates

- Complete-source review: every source line and prose/math region inspected.
- Exact code-fence execution: all 304 Wolfram Language fences extracted byte-for-byte; computational cells evaluated in document order and graphics cells parsed.
- Twenty-example invariant suite: examples 1–20 each have an explicit physics/numerical assertion.
- Shared-core suite: 27 assertions, `OPEN ISSUES: 0`.
- Numerical-method suite: refinement and censoring assertions, `OPEN ISSUES: 0`.
- TeX-to-notebook preflight: all 73 command names supported.
- Notebook structure, rendered visual inspection, fresh independent correctness review, and fresh independent seven-axis quantum-quality review are recorded below when complete.

## Reproducibility record

- Final audited Markdown SHA-256: `6a8e6b30a8f60dfb789f49f088b6024024e14ed1b3521ab98ef4f1d30a068f32`
- Wolfram kernel: 15.0 on macOS ARM64.
- Generated notebook SHA-256: `d5637436c89af3bde54f2574fcc5754aae9e6e8a4d2e117e37903197659b77d1`
- Notebook input-cell parity: 304 Markdown WL fences / 304 notebook Input cells; one Title; 39 section-hierarchy cells; `OPEN ISSUES: 0`.
- Independent correctness verdict: `OPEN ISSUES: 0`. A fresh Wolfram 15.0 `-noinit` audit reproduced all 304 source cells, all 20 example assertions, all 27 shared-core assertions, two fresh-cache numerical refinements, 304/304 notebook Input-cell parity, all 914 TeX spans, direct 83-page rendering, and the final hashes without messages or failures.
- Independent quality verdict: `OPEN ISSUES: 0`. A separate read-only audit passed axes A--G and all five required regimes (general symbolic, exactly solvable special, asymptotic/limiting, numerical reference, and failure/edge), including independent qutrit evolution, amplitude-damping, Wigner/parity, spectrum, invariant, and notebook-semantic checks.
