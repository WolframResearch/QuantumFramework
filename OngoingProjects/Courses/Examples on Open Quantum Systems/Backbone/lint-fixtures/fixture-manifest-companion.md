# Fixture companion: manifests for fixture-manifest-missing.md

<!-- manifest: H -->

## The headline check

The two-route comparison behind H: what varied, what was held fixed, corner cases, clause scope. (Fixture text; the anchor above is what the linter reads.)

The manifest for S1 is deliberately absent: the page's S1 tail claims a numerical check, so the linter must report the missing anchor.
