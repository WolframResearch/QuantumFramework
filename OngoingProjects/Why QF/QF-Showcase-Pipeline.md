# QF Showcase: Example-Design Pipeline and Decision Log

A living tracking doc for *how* the showcase examples are generated, why, and how to make them better. The deliverable is [QF-Showcase.md](QF-Showcase.md); this file is the process behind it.

---

## 1. The pipeline (reusable checklist per claim)

Run every candidate example through these gates, in order. An example that fails an early gate is rejected before any kernel time is spent.

1. **Name the unique mechanism.** What can QF do here that Qiskit / Cirq / PennyLane cannot, or can only do clumsily? Write it in one sentence. If you cannot, the claim has no example.
2. **Demonstrate a property of the object model, not a single convenience function.** A good example shows that the *whole algebra* behaves a certain way (states, operators, circuits, channels), not that one function has a nice option. This is the gate the first draft of Claim 1 failed.
3. **Contrast test.** Would a competent Qiskit user, seeing this, say "I cannot easily do that"? If the result is hand-derivable or one `import` away elsewhere, it is not a differentiator, only a feature.
4. **Audience fit, explicit.** Map the example to the four audiences (student, educator, researcher, industry). State the "so what" for each. If it lands for fewer than two, either redesign or label it as deliberately narrow.
5. **Non-triviality vs legibility.** The example must be too involved to do by hand, yet still readable in under ten lines. Trivial-but-clean is a trap: it reads as "toy."
6. **Verify in kernel.** Run it. Capture the real output. Ship only what passed. No `Quiet`, no `Print` (use natural output / `Get` in `-code`).
7. **Annotate honestly.** One line per audience, and an explicit flag for any audience the example serves weakly.

### Anti-patterns this pipeline is meant to catch
- **Function-as-claim.** Picking the one function whose uniqueness is most obvious (here, `QuantumEvolve` because `DSolve` is visible) and letting it stand in for a property that is actually pervasive.
- **Trivial instance.** Choosing the smallest case (H = X, computational initial state, no free parameter in the model) so that nothing in the example is actually parametric except an incidental variable.
- **Audience drift.** Shipping an example that only a student could love and tagging all four audiences anyway.

---

## 2. Assumptions log

Each row is an assumption baked into a first-draft example, and whether it survived scrutiny.

| # | Claim | Assumption I made | Held? | Correction |
|---|---|---|---|---|
| A1 | 1 | "Symbolic-first" is best shown by symbolic *dynamics*, so use `QuantumEvolve`. | No | Symbolic-ness is a property of the whole object algebra (operators, states, circuits, observables). Evolution is one example, not the headline. |
| A2 | 1 | The smallest instance (evolve $\lvert0\rangle$ under $X$) is "clean and therefore good." | No | It is hand-derivable and has no free parameter in the model. Reads as a toy; hooks no researcher or industry user. |
| A3 | 1 | One example can serve all four audiences. | Partly | True only if the example *builds up* from recognizable (gate matrices) to consequential (closed-form VQE cost). A single trivial instance cannot. |
| A4 | 2 | `QuantumChannel["Operators"]` returns the Kraus list. | No | Undefined for this head; dropped that line, kept the verified density-matrix result. |
| A5 | 3 | Global `VonNeumannEntropy` shows entanglement. | No | It is $0$ for any pure state. Entanglement lives in the reduced state; used concurrence as the witness. |
| A6 | 4 | The Qiskit / IBM / AWS bridges can be verified in this environment. | No | `QuantumCircuitOperatorToQiskit` returns unevaluated (no Python/qiskit). Claim 4 anchored on verified QASM export; the hardware-submission line is shown but explicitly labeled NOT executed. |
| A7 | 2 | `G2Coherence` of a symbolic-`\[Alpha]` coherent state gives a clean closed form. | No | It expands the Fock-truncation series into a huge expression. Used numeric photon statistics instead (Fock $0$, coherent $1$, thermal $2$). |
| A8 | all | One staged example per claim serves every audience. | Superseded | User chose **one example per audience** (4 per claim, 20 total). Applied. |
| A9 | 1 | The per-audience rebuild of Claim 1 covered all three object types the user named. | No (caught on review) | First per-audience rebuild used `QuantumState["0"]` everywhere: no symbolic `QuantumState`. Fixed: Student is now a symbolic state (Bloch), Industry combines a symbolic Hamiltonian with a symbolic initial state. |
| A10 | 1 | The audience axis (student/educator/...) is the right organizing structure for every claim. | No | For Claim 1 the natural axis is the **object type**, because the claim *is* "this property pervades the object model." Audience labels were noise there. Claim 1 re-cut as an object-type sequence (state -> operator -> evolution -> circuit -> composition), labels dropped. Lesson in section 6. |
| A11 | 1 | `QuantumEvolve` was wrong to use at all (per A1). | Refined | A1's error was making `QuantumEvolve` *the headline* of a pervasive property. Using it as **one example** (operator -> its symbolic dynamics) is correct and valuable: it shows $U(t)=e^{-iHt}$ in closed form and ties the evolution frequency $\sqrt{J^2+h^2}$ back to the spectrum. Reintroduced in that role. |
| A12 | 2-5 | The Claim 1 lessons transfer cleanly to the other claims by just dropping audience labels. | Mostly, plus surgery | Claims 3 and 5 needed only re-titling to their natural axis (WL operation / view type). Claim 4 needed *consolidation* (3 near-identical QASM examples collapsed to 2 + an honest bridge example) and a sharpened Claim-4-vs-5 boundary. Claim 2 needed real surgery (next row). |
| A13 | 2 | Claim 2's examples proved "full theory." | No (cross-contamination) | Three of four leaned on *symbolic* results ($1-2p+2p^2$, $\sqrt{1-\gamma}/2$, $\sqrt{1-p/2}$), which is Claim 1's punch, not Claim 2's. Re-cut on **object category** with **numeric** punches: channel purity $1\to0.875$, SIC-POVM 4 outcomes, $g^{(2)}=0/1/2$, Wigner $W(0,0)=-0.318$. Each foregrounds an object a gate-only framework lacks. |
| A14 | 2 | `TracePreservingQ` is safe to showcase for a CPTP channel. | No | Returns `False` for a genuinely trace-preserving amplitude-damping channel at numeric $\gamma$ (QF numerical-tolerance quirk); `KrausOperators`/`Operators` are undefined. Dropped both; used the purity drop instead. Logged so we do not reach for them again. |
| A15 | 2-5 | Strict claim orthogonality (A13: keep symbolic out of Claim 2-5) is the final word. | Overridden by user | User asked to add a symbolic example to every other claim. Resolution that avoids re-creating A13's problem: each added example keeps *its own* claim's identity and adds symbolic as a **secondary** dimension, explicitly labelled "and this is symbolic too". Added: Claim 2 symbolic channel ($\rho(\gamma)$), Claim 3 symbolic dispatch (Werner purity/entropy in $p$), Claim 5 symbolic views (parametric diagram + matrix). **Claim 4 cannot**: QASM/hardware need numeric angles (`["QASM"]` errors on a symbolic angle), stated as the honest boundary. |
| A16 | 1 | The manual braket $\langle\psi\|O\|\psi\rangle$ was the only way to get an expectation. | No (user corrected) | A `QuantumCircuitOperator` accepts **states as elements**: `{psi, op -> 1, psi["Dagger"]}` compiles to the scalar $\langle\psi\|O\|\psi\rangle$. More idiomatic and more compelling for Claim 1 (the *object itself* is the braket, all symbols). Adopted in the final example. Lesson: ask whether the framework already expresses the thing as an object before hand-rolling it. |
| A17 | 1 | A sequence of *small* symbolic examples is enough to prove "symbolic-first." | No (too trivial) | Every example was single-qubit / $2\times2$: fine as principle, unconvincing to a serious reader. Added a **capstone**: a 2-qubit Heisenberg quench ($H=J(XX{+}YY{+}ZZ)$), symbolic evolution $\|\psi(t)\rangle = e^{iJt}[\cos 2Jt\,\|01\rangle - i\sin 2Jt\,\|10\rangle]$, and the entanglement entropy $S(t) = -\cos^2 2Jt\,\log_2\cos^2 2Jt - \sin^2 2Jt\,\log_2\sin^2 2Jt$ as a closed-form function of time. **Lesson: a uniqueness claim needs at least one example that is hard or impossible on the competitor**, not only clean small ones. The contrast-test win here is concrete: numeric tools give $S$ at one $t$; QF gives the whole $S(t)$. Verified `matches_QF -> True`. |
| A18 | all | Framing QF *against* numeric platforms ("Qiskit can't", "no numeric platform can match") is the right register. | No (user steer) | User wanted a positive feature tour, not a comparison. Removed all "cannot / impossible / structurally / not just" language; retitled Claim 1 (dropped "not numeric-first") and Claim 2 (dropped "not just gate circuits"); "Unique mechanism" -> "Key feature". Qiskit/Cirq survive only as *interop targets* (Claim 4). **Lesson: lead with what the tool does, not what others lack.** |
| A19 | all | Embedding rendered plot images is wanted now. | No (user steer) | User: "do not include plots yet, only the WL code that generates them." Removed all 12 image embeds; added the missing plotting code (Claim 1 $S(t)$, Claim 2 $g^{(2)}$ bar chart + Wigner `Plot3D`). Doc is now text + code only. PNGs remain on disk, unreferenced. |
| A20 | all | "examples/sequence" wording: original draft used "rungs"/"ladder". | No (user steer) | User dislikes "rungs". Replaced every "rung(s)"/"ladder" with plain "example(s)"/"sequence"/"builds up". |
| A21 | all | Deliverable lives at repo root. | No | User created `OngoingProjects/Why QF/` and asked files to live there. Moved both `.md` files + `img/showcase/`; kept the `img/showcase/` subpath so links and cross-references resolve with no edits. **Recurring hazard: the PNGs vanished from disk twice between turns** (folder is git-untracked; likely an external sync). Regenerate from `/tmp/qfshow/regen_final.wls` if missing. |
| A22 | 4 | Memory/audit already documents the interop API I should use (`QuantumQASM`, `QiskitTarget`, `IBMJobSubmit`, `IBMJob`). | No (verified) | These live in **new kernel files** (`QuantumQASM.m`, `IBMQuantum.m`) added *after* the `bfddf5ed` audit anchor; the audit re-synced today (`ac32bda8`) covers them but `IBMQuantum.m` then drifted **+151 lines** to HEAD `541a104b`. Verified signatures in live source + kernel; native `QuantumQASM` import/export is pure-WL and now **runnable/verified** (not just illustrative). Wrote memory `reference_qasm_ibm_interop_hub`. **Lesson: for any symbol newer than the audit anchor, read the source; memory is a pointer, not the API.** |
| A23 | 6 | The five claims cover QF's distinctive features. | No (user addition) | User: basis handling is a distinct unique feature. Added **Claim 6** (basis-awareness + automatic cross-basis reconciliation), axis = *basis*. Verified the basis API in-kernel first (not from memory). |
| A24 | 6 | First Claim 6 examples (Bell state, `XX+YY+ZZ` Pauli decomposition) were good enough. | No (too trivial) | User flagged them as trivial and asked for exotic/phase-space bases + learning-by-computing style. Re-cut: Dicke $\|D_4^2\rangle$ ($\mathrm{Var}\,S_z=0$ in the spin basis) for "one object, many bases"; the **H$_2$ molecular Hamiltonian as a dense matrix** -> 6 Pauli terms; **discrete Wigner negativity** ($-1/3$, magic resource) and **SIC/QBism** $\{\tfrac12,\tfrac16,\tfrac16,\tfrac16\}$ as the exotic/phase-space bases. Rewritten in concept->bridge->cell->interpretation form (learning-by-computing skill), one computation per cell. Literature-grounded (Howard 2014, Fuchs-Schack 2013, Gokhale 2020, Adamson-Steinberg 2010). |
| A25 | 1 | The static Hamiltonian $JZ+hX$ + its $e^{-iHt}$ is a strong enough "symbolic Hamiltonian + evolution" example. | No (user upgrade) | User: make it a **time-dependent** $H(t)$, non-trivial, with a recoverable propagator, ideally with $[H(t_1),H(t_2)]\ne0$. Replaced both sections with a spin in a **rotating field** (magnetic resonance): $H(t)=\tfrac{\omega_0}{2}Z+\tfrac{\Omega}{2}(\cos\omega t\,X+\sin\omega t\,Y)$. Verified $[H(t_1),H(t_2)]\ne0$ (diagonal $\mp\tfrac{i}{2}\Omega^2\sin[(t_1-t_2)\omega]$), `QuantumEvolve[H,None]` solves the time-dependent SE ($U(0)=I$, residual $0$, unitary), and the transition probability is the exact **Rabi formula** $\tfrac{\Omega^2}{\Omega^2+\delta^2}\sin^2(\tfrac t2\sqrt{\Omega^2+\delta^2})$. **Gotcha:** plain `FullSimplify` leaves $P$ in exp form; `Assuming[reals, FullSimplify[ComplexExpand[...]]]` is needed for the clean sine. `\[FormalT]` is QF's time variable inside the operator. **Commutator cell** (user request): `Commutator[Hm[t1],Hm[t2],Dot]` (verified `=== a.b-b.a`). Doc-page checked: 2-arg `Commutator[x,y]` uses `**` (won't evaluate for matrices), so the 3rd algebra arg `Dot` is required; `Commutator` is **new in WL 14.3 (2025)**: fine for the showcase's 15.0, but not available on older kernels. |
| A26 | 1, all | The Rabi example is the only time-dependent case worth showing, and the doc already reads as learning-by-computing. | No (user additions) | (a) Added **Landau-Zener** $H(t)=\tfrac{vt}{2}Z+\tfrac{\Delta}{2}X$ as a second time-dependent case where `QuantumEvolve` returns the propagator in **`ParabolicCylinderD`** (Weber functions): the escalation "elementary Rabi -> special-function Landau-Zener". Verified $U$ valid (numeric $U(0)=I$, SE, unitary); stated the $e^{-\pi\Delta^2/2v}$ limit but **did not** include a numeric match cell (finite-$T$ Stückelberg oscillation: $0.481$ vs $0.463$). (b) **Doc-wide learning-by-computing pass**: split multi-computation cells (Claim 1 parametric circuit -> unitary cell + expectation cell; Claim 5 every two-view cell -> one view per cell), added a bridge sentence before and an interpretation after each cell, fixed Claim 5's orphaned graphics outputs. One computation per cell now holds throughout. |
| A27 | 1 | The $R_Y(\theta)R_Z(\phi)\to\langle Z\rangle=\cos\theta$ parametric example is good enough. | No (too trivial, user asked to pick a research ansatz) | From a PhD-level list (HEA, UCCSD, ADAPT-VQE, HVA, QAOA, ...) the showcase needs an ansatz that (a) is recognizable and (b) yields a *clean closed-form* cost landscape (Claim 1's symbolic message). Chose **QAOA** ($p{=}1$ MaxCut on a triangle): cost layer $e^{-i\gamma ZZ}$ (`"R"[2\[Gamma],"ZZ"]`) + mixer `RX`$[2\beta]$. QF returns the exact **Farhi landscape** $\langle C\rangle=\tfrac38(3+\cos4\beta+2\cos4\gamma\sin^2 2\beta-2\sin4\beta\sin4\gamma)$; `NMaxValue` $=2$ (the triangle's optimal cut). **Gotchas:** the raw $\langle\psi|C|\psi\rangle$ blows up unless `ComplexExpand` is applied; the cost operator must be built as an 8x8 matrix (`"I"[2]` is a *1-qubit* identity, mismatched against 2-qubit `"ZZ"`). HVA is the physics-flavored alternative (ties to the Heisenberg capstone). Also fixed the now-dangling $\langle\psi|U|\psi\rangle$ side note in the bra-ket example (it referenced the deleted RY/RZ unitary). |
| A28 | 1 | The hand-built QAOA (explicit gates `"H"->1,...`, `IdentityMatrix[8]` cost, `Conjugate[v].C.v`) is the best form. | No (user pointed to the talk) | The China-2026 talk notebook has a far cleaner **graph-driven, no-hardcoding** QAOA. Adopted its method: `g = CompleteGraph[3]` (triangle, *frustrated* so MaxCut $=2<3$); `Hc`, `qcost`, `mixer`, `stateprep` all built from `EdgeList[g]`/`VertexList[g]`/`VertexCount[g]`; cost landscape via the **bra-ket circuit** `(oneLayer /* Hc /* oneLayer["Dagger"])[]["Scalar"]` (no manual inner product, and no `ComplexExpand` needed); classical reference via `FindMaximumCut[g]`. Verified in the current kernel: landscape $=\tfrac38(3+\cos2\gamma+2\cos2\theta\sin^2\gamma-2\sin2\gamma\sin2\theta)$, `NMaximize` $=2$. **Two drift fixes vs the April-2026 talk:** the talk's list-form parametrized gate `{"R",\[Gamma],"ZZ"}` is dead in 2.0.0 -> call-form `"R"[\[Gamma],"ZZ"]`; otherwise the talk's `QuantumOperator["ZZ"/2,#]` scaled-operator and the `/*` + `[]["Scalar"]` pipeline run unchanged. The talk used $K_{2,2}$ (bipartite, trivial all-edges cut); switched to the triangle for genuine frustration at minimum size. |
| A29 | 1 | More forward time-dependent cases (spin-$j$, Rosen-Zener, ...) would strengthen Claim 1. | No (tested, they don't deliver) | User shared a PhD survey of analytic $H(t)$. **Tested in QF:** `QuantumEvolve`/`DSolve` solves only the 2x2 exactly-solvable models (Rabi, Landau-Zener, already shown); it **fails** on spin-1 rotating field and Rosen-Zener (sech), and QF's named `"JX"[1]`/`"JZ"[1]` both return `diag(-1,0,1)` (unusable spin-$j$). So no clean *forward* addition exists. The one clean **inverse** class (the survey's #1, $H=i\dot U U^\dagger$) was added instead: design $U(t)=e^{-i(t^2/2)Z}e^{-i(t/2)X}$ -> recover $H(t)=tZ+\tfrac12(\cos t^2\,X+\sin t^2\,Y)$, verified Hermitian / non-commuting / solves the SE. No solver involved (pure symbolic calculus); research hook = counterdiabatic driving / STA. Completes the arc: QF *solves* the dynamics (Rabi, LZ) and *designs* it (inverse). **Lesson: filter a wishlist by what the tool actually returns, not by what is mathematically exact.** |
| A30 | all | After the earlier learning-by-computing pass (A24/A26), the cells were granular enough. | No (user: "too many codes in a code cell") | Full re-read + a density pass: **one computation per cell** now holds across all 66 code cells (heuristic check: none with 3+ statements). Split the 4-statement offenders (Claim 1 QAOA `oneLayer` -> stateprep/qcost/mixer/compose; Claim 1 bra-ket -> Hamiltonian+state / assemble) and the 3-statement ones (symbolic state, Heisenberg payoff, Claim 3 `Reduce`/`Dataset`, Claim 4 target, Claim 6 variance), each with its own bridge + interpretation. **Inlined** two cells to drop helper assignments entirely (entropy `Plot` formula; purity before/after as a single list). Pure splits need no re-verification (same statements, shared variables top-to-bottom); the two changed cells (QAOA `U`-definition fold, inlines) were re-verified. |
| A31 | 1 | The "circuit can contain states ($\langle\psi\|H\|\psi\rangle$ in one object)" deserves its own section. | No (user: it's already in QAOA) | The QAOA landscape already uses exactly that idiom (`(oneLayer /* Hc /* oneLayer["Dagger"])[]["Scalar"]`), so the dedicated section was redundant. **Removed** it and instead **named the feature explicitly in the QAOA prose** ("a `QuantumCircuitOperator` can hold states as elements; prep/operator/`["Dagger"]` builds $\langle\psi\|H_C\|\psi\rangle$ as one object", plus the general "any operator or circuit between ket and bra" line folded over from the deleted section). Minor loss: the standalone VQE point (min $-\sqrt{a^2+b^2}=$ eigenvalue) is gone, but QAOA's "max $=2=$`FindMaximumCut`" carries the same "optimize the closed form exactly" message. No dangling refs (`Hab`/`braket` unused downstream). **Lesson: name the reusable idiom where it first does real work, don't re-teach it.** |
| A32 | 2 | The open-system example should be numeric ($\{1,0.875\}$, per A13), with the symbolic-channel point in a separate trailing subsection (A15). | Reversed by user | User supplied a richer **symbolic** open-system example and asked it to replace the numeric one: a generic Bloch state $\cos\tfrac\theta2\|0\rangle+e^{i\phi}\sin\tfrac\theta2\|1\rangle$ (purity $1$) through amplitude damping loses purity $2\gamma(1-\gamma)\sin^4\tfrac\theta2$ (verified; $\phi$-independent; zero at $\gamma=0,1$ and $\theta=0$). **Cleaned the user's WL** (state defined once; over-broad assumptions trimmed; shown as the *purity loss*, which `FullSimplify` returns cleanly, vs. the busier $\cos2\theta$ form of the purity itself). Two consequences handled: (a) revised the Claim 2 intro (dropped "each result is deliberately numeric"), (b) **removed the now-redundant trailing "...symbolic too" subsection** (it was also symbolic amplitude damping; the new example carries that role, satisfying A15 with one example, not two). Claim 2 is now 4 clean categories. A13's anti-contamination concern is consciously relaxed here: one symbolic open-system result, framed as scope+symbolics composing. |
| A33 | 2, 3, 4 | (a batch of user edits) | Applied + verified | **POVM (2):** added a line that any POVM is buildable from a list of effects (`QuantumMeasurementOperator[{E1,E2,...}]`, verified `POVMQ` on a 2-effect example); SIC stays the worked example. **Dropped "Straight into `Dataset`" (3)** per user; updated the Claim 3 intro built-in list (`Dataset`->`NProbability`). **Export (4):** noted OpenQASM **2.0** is supported, import is native for 2.0/3.0, a 2.0 *dump* needs the qiskit path (`QuantumQASM[qc,"Version"->2]`); native export stays 3.0. **Import (4):** replaced the toy Bell `h;cx;` with the user's **4-qubit CHSH circuit** (reset/ctrl/negctrl/measure, imported natively as `qc`), then the one-liner `NProbability[BitAnd[x,y]==BitXor[a,b],{a,x,y,b}\[Distributed]qc[]["MultivariateDistribution"]]` = **0.8536 = $\cos^2\tfrac\pi8$ = Tsirelson bound** (verified). **IBM (4):** the QPU result is the **same `QuantumMeasurement` object** as a sim, so one `["ProbabilitiesPlot"]` charts both; show ideal-sim vs QPU side by side (sim runnable + verified `Graphics`; QPU not executed). Gotcha: `qc[]["ProbabilitiesPlot"]` on a `{"M"}` circuit warns `MatrixPower::matsq` (returns a `QuantumState`, not a clean measurement); use `QuantumMeasurementOperator[Range[n]][state]` for a clean `QuantumMeasurement`. The "merge symbolic-too + open-system" ask was already done in A32. |
| A34 | 4 | "Symbolic parameters resolve at the boundary" is a fine standalone subsection. | No | It was the **only cell-less subsection** in the doc (a bare prose claim), and it can't be cleanly demonstrated, `QuantumQASM` on a symbolic `"RY"[\[Theta]]` circuit does return a `Failure` (verified, tag `ConfirmationFailed`) but leaks a `StringJoin::string` message first, so a demo cell would be noisy (and no-`Quiet`). **Folded the point into the export note** ("a circuit with a symbolic parameter returns a `Failure`… resolve at the boundary, keep symbolic elsewhere") and **removed the standalone subsection**. Claim 4 is now 4 subsections, each with a runnable cell. |
| A35 | 4, 5 | Keep the IBM inline honesty note and the multiway-graph view. | No (user dropped both) | **Dropped the IBM "Honesty note"** paragraph; to keep the doc honest, folded its substance into the **Reproducibility** note (Claim 4 transpile / `IBMJobSubmit` / device chart need qiskit + token, not run here; signatures verified). **Dropped the multiway-graph example** (`QuantumCircuitMultiwayGraph[...]`, "not useful"); the Claim 5 section is now "As a tensor network" (single view), leaving Bloch / diagram+matrix / tensor network / histogram. |
| A36 | 5 | The "many views" readout view is a measurement histogram of the GHZ state (`BarChart[Values[probs], ...]`). | Replaced by user | Swapped the `BarChart`-of-probabilities for the object's own chart of **complex amplitudes**: `QuantumCircuitOperator[{"GHZ"[3], "Fourier"[3]}][]["AmplitudesChart", ChartLegends -> Automatic]` (verified: applies to `\|000\rangle`, returns a `Legended` chart of the 8 amplitudes of QFT$\|{\rm GHZ}\rangle$ = $\{0.5,\,0.427-0.177i,\,0.25-0.25i,\,\dots\}$). Net gains: (a) shows a **first-class object method** (`["AmplitudesChart"]`) instead of hand-rolled `BarChart`+manual `ChartLabels`, fitting the "many views of one object" theme better; (b) the QFT injects **nontrivial phases**, so the view shows magnitude *and* phase, which a probability histogram discards. Section heading "As a measurement histogram" -> "As an amplitudes chart"; interpretation rewritten. |
| A82 | 1 (Landau-Zener) | The LZ survival cell (A78) computed `U(t,-T)` by raw matrix algebra (`Inverse[U/.t->-10].{1.,0.}`, `ListLinePlot`). | No (user supplied an idiomatic QF-operator version) | **Survival cell swapped to the user's QF-object form.** The propagator display cell now binds `matULZ = FullSimplify[Normal[ULZ["Matrix"]]]`; the survival cell wraps it as a parametric operator `op = N@QuantumOperator[matULZ /. {v->1., Δ->Sqrt[g]}, "Parameters"->{g, FormalT}]` and builds the state by composition: `c = op[g,-10]["Dagger"][]`, `survival[g_,t_] = Abs[(QuantumState["0"]["Dagger"]@op[g,t][c])["Scalar"]]^2`, then `Plot[Evaluate@Table[...]]`. Verified identical physics (start 1, end {0.872,0.409,0.033}, ringing; cross-checks the A78 curves). `\.b2` kept for the legend `Δ²` (the literal `²` mojibakes to `Â²` through `wolframscript -file`). Slower (~36 s vs 16 s, adaptive Plot + operator composition) but acceptable. Suite green. |
| A83 | 1 (QAOA) | QAOA used the triangle `CompleteGraph[3]`, whose p=1 landscape max *provably equals* the true cut (both 2). | No (user: go with `CompleteGraph[4]`, make everything consistent) | **QAOA switched to $K_4$ with a corrected, more honest narrative.** Computed the full K4 case: `FindMaximumCut` = 4 (2-2 split of 6 edges); landscape $\frac38(7 + \cos2\theta + 2\cos4\gamma\sin^2\theta - 8\cos^2\gamma\sin\gamma\sin2\theta)$; $\gamma{=}0$ flat at 3 (= $\|E\|/2$); **periodicity now asymmetric, $\pi$ in $\theta$ but $2\pi$ in $\gamma$** (the $\sin\gamma$ term); `Maximize` $\approx 3.70 < 4$. KEY consistency fix: the triangle's "variational optimum *provably* equals the true cut" was a lucky special case; for K4 **p=1 falls short** (best expected cut $\approx3.70$, a cubic root, vs true 4). Punchline rewritten honestly (single layer cannot reach the optimum; the exact landscape certifies the ceiling as a closed-form algebraic number; deeper QAOA closes the gap; the surviving showcase point is exact symbolic optimization). Closing-recap "maximum provably equals the true cut" -> "exact maximum is a closed-form cubic root". Prose quotes the value generically ("a root of a cubic $\approx3.70$"), NOT a specific polynomial (the raw `Maximize` output is $\frac38(7 - \mathrm{Root}[4x^3+47x^2-142x-697])$, a different form than a `FullSimplify` cubic; describing generically keeps prose matching the cell). Diagram (4 qubits, 6 ZZ), Plot3D, landscape (1.5 s), Maximize (0.4 s) all green. |
| A85 | 3 (teleportation) | Teleportation verified via the Bloch-vector DIFFERENCE (`...["BlochVector"] - psi["BlochVector"]` -> $\{0,0,0\}$), one cell with `ComplexExpand`. | No (user supplied a two-cell roundtrip-first version) | **Teleportation verification split into roundtrip-first form** (per [[feedback-user-facing-roundtrip-first]]). Cell A reads the INPUT Bloch vector `FullSimplify[psi["BlochVector"], {θ,φ ∈ Reals}]` -> $(\sin\theta\cos\phi, \sin\theta\sin\phi, \cos\theta)$; cell B runs the protocol and reads BOB's `FullSimplify[QuantumPartialTrace[out,{1,2,3,4}]["BlochVector"], ...]` -> the **same** closed form (verified `===` identical, no `ComplexExpand` needed). Shows the recovered state IS the input, not merely that a difference vanishes. Prose rewritten to present both vectors. +1 cell. Suite 94 green. |
| A86 | 3 (QEC) | The QEC circuit used verbose gate specs (`QuantumOperator["RX"[2θ],{2}]`, `QuantumOperator["C"[QuantumOperator["X",{1}],{0},{-1}]]`), no diagram displayed. | No (user supplied a tighter form + `qec["Diagram"]`) | **QEC circuit tightened + diagram shown.** Shorthand `"C"["X", {0}, {-1}]` (X on default qubit 1), `"C"["X" -> n, ...]`, `"RX"[2θ] -> 2`, default `"CNOT"`, and `"Label" -> "Encode"` on `enc`; cell now ends in `qec["Diagram"]`. Verified functionally IDENTICAL: syndrome weights $\{\cos^2\theta, 0, 0, \sin^2\theta\}$, fidelity exactly $1$, diagram renders (`enc` arity 3). Prose updated (`"C"["X", ones, zeros]` shorthand; added a sentence describing the 5-wire diagram: labeled Encode block, $R_X(2\theta)$ error, two parity measures onto records $0/-1$, three record-controlled corrections). Suite **94 cells** green, 0 messages. |
| A87 | 2 (qudit $d\ge3$) | Claim 2's $d\ge3$ content was static and confined (spin-1 ZFS + 5-site ring): no "impossible on a qubit" punch, nothing on contextuality, magic, or single-qudit algorithms. | No (user: KCBS lead, magic coda, fuller Grover) | **Three kernel-verified qudit examples added to Claim 2**, sourced from [Qudit-Showcase-Circuits.md](../Higher-Dimensional%20Circuits/Qudit-Showcase-Circuits.md). **(a) Magic coda** appended to the phase-space (Wigner/Husimi) section: discrete qutrit Wigner via `qs["PhaseSpace"]`, sum-negativity `{0,0,1/3}` (stabilizers $\|0\rangle,\|+\rangle$ flat, "Strange" $(\|1\rangle-\|2\rangle)/\sqrt2$ negative; Gross discrete-Hudson = nonneg iff stabilizer); Strange Wigner `{{-1/3,1/6,1/6},...}` (lone $-1/3$ at origin, sums to $1$); mana $\log(5/3)\approx0.511$; Clifford `SUM[3]` transport `{0,1/3}` (moves but never creates magic). Honest $d{=}2$ caveat (no clean Wigner-stabilizer equivalence). **(b) KCBS contextuality** as the lead standalone qudit subsection: five cone projectors, $\sum_{i=0}^4\langle\Pi_i\rangle=\sqrt5\approx2.236>2$, exact orthogonality `{0,0,0,0,0}`, "impossible for a qubit" ($\mathbb{C}^2$ always noncontextual, $d{=}3$ minimal). **(c) Single-qudit Grover** ($d{=}8$): `"Fourier"[8]` DFT init + $\lfloor\tfrac\pi4\sqrt8\rfloor{=}2$ oracle/diffusion reflections, marked prob exactly `121/128`, max gate arity `1` (zero entanglement, the dimension does entanglement's work). **Placement:** magic = phase-space coda (extends continuous→discrete Wigner); KCBS+Grover = new subsections between phase-space and basis, forming a qudit cluster (Wigner negativity → contextuality → entanglement-free search). **Storytelling fixes:** roadmap bullet + Claim 2 intro ("quiet generality"→loud) + closing recap all enriched with the three punches ($\sqrt5$, $-\tfrac13$ Wigner, 8-level Grover); "Any finite dimension" opening reworded to flow from the accumulated qutrit content (no longer reintroduces "spin-1 is a three-level system" cold); "magic (Bell) basis" → "Bell-derived basis" to avoid colliding with the fresh magic-STATE term. Self-contained (`QuantumBasis[{d}]`/`QuantumBasis[3]` throughout; new short names `d/w/phi/th/meas/axis` verified unique, no leak). Suite **103 cells** green, 0 messages (full one-kernel run, document order). |
| A88 | 2 (phase-space viz) | The cat-state Wigner/Husimi figure used `GraphicsRow[..., ImageSize -> 560]` with `ColorFunction -> "TemperatureMap"`. | No (user supplied a `Row[{Plot3D, Plot3D}]` form) | **Figure swapped to the user's `Row` form**: two `Plot3D`s, `ColorFunction -> "SunsetColors"`, per-plot `ImageSize -> Small`, Wigner over `{p,-2,2}`, Husimi over `{p,-3,3}`. **Domain fix:** the `husimi` interpolating function was built on $p\in[-2,2]$; plotting over $[-3,3]$ extrapolates and fires `InterpolatingFunction::dmval` (suite-forbidden), so the build window widened to `{-6,6},{-3,3}` (one prose half-sentence added, "a touch taller in $p$ since the Husimi spreads wider"). `husimi[0,0]` unchanged at $0.01165\approx0.012$; Wigner build untouched (`+0.318`/`-0.236` bit-identical). QAOA-landscape `Plot3D` deliberately left on `TemperatureMap` (out of scope). Suite green, 0 messages. |
| A89 | 2 (KCBS) | The KCBS cell hardcoded the geometry: `phi[i_]`/`vec[i_]` per-component trig, `proj[i_] := KroneckerProduct[...]`, explicit `Mod`-indexed orthogonality, one packed cell. | No (user: too much hardcoding, do it in WL/QF, follow learning-by-computing) | **KCBS rewritten as 3 learning-by-computing beats, de-hardcoded with built-ins.** (1) Rays as a *rotation orbit*: `rays = Table[RotationMatrix[4 Pi k/5, {0,0,1}] . {Sin[th],0,Cos[th]}, {k,0,4}]` (one seed turned by the pentagram step; verified `rays ==` the old `vec[i]` exactly), displayed via `MatrixForm` (constant 3rd column = cone height $5^{-1/4}=0.6687$). (2) Compatibility via `Chop @ N @ MapThread[Dot, {rays, RotateLeft[rays]}]` $\to$ `{0,0,0,0,0}` (no explicit `Mod`). (3) Measurement sum `Total[QuantumMeasurementOperator[N @ Outer[Times,#,#], {1}, QuantumBasis[3]][apex]["Mean"] & /@ rays]` $\to \sqrt5$. **TRAP found + avoided:** building rays as `QuantumState[v, QuantumBasis[3]]` and scoring via `QuantumDistance[.., "Fidelity"]` or `QuantumMeasurementOperator[state, {1}]["Mean"]` gives WRONG answers (compat $\approx1$, sum $10$ / $1.656$): a QMO built from a *state* weights computational eigenvalues $\{0,1,2\}$ (not $\langle\Pi\rangle$), and the fidelity route mis-binds the qutrit basis. The verified mechanism is the projector-MATRIX QMO `["Mean"]` ($=\mathrm{Tr}[\rho\Pi]$); only the classical geometry was de-hardcoded (pure WL `RotationMatrix`/`Outer`/`RotateLeft`/`Map`). Net +1 cell (KCBS 2$\to$3); `axis`$\to$`apex`, dropped `phi/vec/proj/meas`. Suite **104 cells** green, 0 messages. |
| A90 | 2 + global | Full-document audit ("losing the big picture"): Claim 2 had grown to 11 subsections / ~32% of the body, and its three qutrit additions (A87) read as physics vignettes rather than object-model evidence; plus a ghost "triangle" reference in QAOA (leftover from the pre-$K_4$ example, A83), uneven inter-claim seams (only 3$\to$4 had a bridge), and several over-long passages. | Yes (user: implement all audit suggestions, save the qudit extras separately) | **Major refocus pass.** **(1)** The three extended qudit examples (discrete-Wigner magic, KCBS contextuality, single-qudit Grover) **removed from the showcase and preserved verbatim+verified** in companion [QF-Showcase-Qudit-Extras.md](QF-Showcase-Qudit-Extras.md) (own Setup; runs standalone, 9 cells, 0 messages). Claim 2 back to **8 object-model subsections** (channel, master eq, POVM, optics, Wigner/Husimi, basis, spin-1/ring, tomography). Connective tissue rewired so nothing dangles: Claim 2 intro, roadmap bullet 2, the spin-1/ring opening (now flows from the basis subsection's spin-1 state instead of "already carried contextuality and magic"), and the closing recap all de-qudit-ified. **(2)** Ghost **"triangle" fixed** in the QAOA caveat. **(3)** Inter-claim **transitions added** at 1$\to$2, 2$\to$3, 4$\to$5, 5$\to$6, 6$\to$7 (deep$\to$wide; estimate$\to$open-up measurement; substrate; read$\to$draw; inside$\to$out), making the ladder explicit. **(4)** **Claim 5 reframed as the keystone** (intro now names it the substrate under Claims 1/4, matching the closing's "one mechanism behind all seven"). **(5)** Prose tightened: Landau-Zener special-function paragraph, QAOA caveat, Claim 6 catalog (one 7-line sentence $\to$ three); the g$^2$ `BarChart` got its missing interpretation; Claim 5's `==` opener got a forward link. Judged acceptable as-is, NOT changed: the `==` opener stays (a deliberate gentle lead). Cell count **104$\to$95**; balance now **6/8/3/3/6/5/4**; suite green, 0 messages. |
| A91 | 1 (rotating field) | The "time-dependent symbolic Hamiltonian" + "propagator" cells used `H = ...` with the implicit `\[FormalT]` time variable, a *matrix-lowered* commutator (`Hm[t_] := Normal[H["Matrix"]]...; Commutator[Hm[t1], Hm[t2], Dot]`), and the 2-arg `QuantumEvolve[H, None]`. | No (user supplied a cleaner version) | **Rotating-field code swapped to the user's `h`-as-function form.** `ClearAll[h]; h[t_] := ...` (explicit time `t`); commutator now `Commutator[h[t1], h[t2]]["Matrix"]` acting *directly on the operator objects* (the 2-arg QF `Commutator` overload, matching Claim 5; verified bit-identical to the old matrix, diagonal $\mp\tfrac{i}{2}\Omega^2\sin[(t_1-t_2)\omega]$ plus the same off-diagonals); propagator `u = QuantumEvolve[h[t], None, t]` (3-arg, explicit time var). Propagator matrix and transition probability verified **bit-identical** to the prior `\[FormalT]` version (lab-frame Rabi propagator; Rabi formula $\Omega^2\sin^2(\tfrac t2\Omega_R)/\Omega_R^2$). **One-char typo fixed:** the last `Assuming` had `u \[Element] Reals` (asserting the *propagator* is real, meaningless) $\to$ `t \[Element] Reals`; output identical either way since `ComplexExpand` already treats `t` as real, so purely cosmetic. **OPEN / flagged to user:** the two time-dependent examples now use different time-variable conventions, rotating-field explicit `t` vs Landau-Zener implicit `\[FormalT]`; left as-is (change scoped to the two named sections), offered for optional unification. Prose updated (h-as-function, Commutator-on-objects, 3-arg call). Suite **95 cells** green, 0 messages. |
| A92 | 1 (Landau-Zener + Heisenberg) | After A91, the other two Claim 1 time-dependent examples still used implicit `\[FormalT]`, leaving Claim 1 internally inconsistent (`t`/`t`/`\[FormalT]`); the LZ survival code's `"Parameters" -> {g, \[FormalT]}` vs its `t` function arg was the "very odd" usage the user flagged. | Yes (user supplied LZ `t` code; Heisenberg unified for intra-claim consistency) | **Claim 1 fully unified to explicit `t`.** **Landau-Zener:** `HLZ = v t/2 ...`, `ULZ = QuantumEvolve[HLZ, None, t]` (3-arg), `op` params `{g, t}`. Verified **bit-identical** to the `\[FormalT]` version: `matULZ[[1,1]] = e^{-i v t^2/4}{}_1F_1(\tfrac{i\Delta^2}{8v};\tfrac12;\tfrac i2 v t^2)` (now matching the prose's `t`), survival `{1,1,1}` at $t{=}-10$ $\to$ `{0.872,0.409,0.033}` at $t{=}10$ (rings around asymptotes `{0.855,0.390,0.081}`), plot `Legended`, 0 messages. The triple-`t` (operator parameter / `survival[g_,t_]` arg / `Plot` var) resolves correctly by positional substitution. **Heisenberg quench** (proactively unified, same time-variable category + same claim): `QuantumEvolve[H, QuantumState["01"], t]`, entropy assumption `t J \[Element] Reals`, plot drops the `\[FormalT] -> t` rename. Same evolved ket + binary-entropy formula; the `Infinity::indet` from an explicit boundary eval (`0\cdot\log 0`) does NOT fire in the `Plot` cell. **MOJIBAKE GUARD:** the user's pasted legend used a literal `²` (UTF-8 `0xC2 0xB2`), which `wolframscript -file` mis-decodes to `ΔÂ²/v` (`ToCharacterCode["²"]={194,178}`; output `\[CapitalDelta]A^\262/v`); kept the existing `\.b2` escape, which renders as `²` in both the FE notebook and the suite. **STILL `\[FormalT]`, FLAGGED not changed:** Claim 5's four cells, each also mismatching its own prose symbol, Exp propagator (568, prose `e^{-iXt}` $\to$ `t`), H-recovery (588-589, prose `U(t)=R_Z(t^2)R_X(t)` $\to$ `t`), state calculus (597-598, "functions of time" $\to$ `t`), and the `Maximize` (605-607, prose `R_Y(\theta)`/$\theta{=}\pi/4$ $\to$ should be `\[Theta]`, an ANGLE not time). Offered for a follow-up. Suite **95 cells** green, 0 messages. |
| A93 | 4 (framing) + 5 (FormalT) | (a) Claim 4 is titled "three computational engines" but only the tensor-network and stabilizer engines get worked examples; the exact state-vector path (`Method -> "Schrodinger"`) is named in the intro and never separately shown. (b) The four Claim 5 `\[FormalT]` holdouts from A92, each mismatching its own prose symbol. | Yes (user: clarify Claim 4 focus; audit out *all* `\[FormalT]`) | **(1) Claim 4 intro reframed** (minimal, no new cells): the exact state-vector path is "the dense baseline, a literal $2^n$ matrix-vector product available on demand"; "the two engines that genuinely change the *representation*, and so repay a closer look, are the tensor-network default and the stabilizer tableau, which we take in turn," then the interconversion. Sets reader expectations that TN + stabilizer are the focus. **(2) Claim 5 `\[FormalT]` audited out:** Exp propagator (568), H-recovery (588-589), state calculus (597-598) $\to$ `t` (each prose already used `t`: $e^{-iXt}$, $U(t)=R_Z(t^2)R_X(t)$, "functions of time"); the `Maximize` in "The scalars flow into solvers" (605-607) $\to$ **`\[Theta]` not `t`**, since that `\[FormalT]` was a rotation ANGLE whose prose already reads $R_Y(\theta)$, $\theta=\pi/4$ (flagged to user; using `t` would have meant calling an angle "time"). Verified all four with the new symbols, bit-identical (Exp $e^{-iXt}$, H-recovery $tZ+\tfrac12(\cos t^2 X+\sin t^2 Y)$, harmonic $\{0,0\}$, energy $\cos\theta+\sin\theta$ max $\sqrt2$ at $\theta=\pi/4$), 0 messages, including under the master-equation's global `$Assumptions` `t >= 0` that now reaches the `t` cells (results unchanged). **`\[FormalT]` now 0 across both content files** (showcase + qudit-extras; pipeline log keeps it only as documentation). **Process catch:** a failed compound shell command skipped a re-extract, so an interim run executed stale `\[FormalT]` cells (caught: output read `\[FormalT] -> Pi/4`); re-extracted and re-ran clean. Suite **95 cells** green, 0 messages. |
| A94 | 4 (TN-engine example) | The TN-engine subsection used a 12-qubit QFT (`"Fourier"[12]`). User wanted "a circuit users care about, not Fourier", large, that pushes the TN path application. | Yes (user: Grover; investigate + suggest; then "go back to Grover" after I mis-flagged it) | **QFT replaced by a verified Grover search.** Long investigation: benchmarked Grover / QAOA-on-3-regular-graph / 1D-Trotter for TN fit. Key kernel finding (and my CORRECTED mistake): `QuantumCircuitOperator["Grover"/"GroverPhase"[bf]]` is the Grover ITERATION operator $G=D\,O$, NOT the whole algorithm; I wrongly applied it once to `|0..0>` (which reflects weight back onto all-zeros, looks "broken") and reported a bug. User pointed to `OngoingProjects/NamedOperators Audit/Grover-Named-Circuit-Usage.md` (verdict CORRECT-USAGE-FOUND): run it `Nest`-style on the uniform superposition. Also: `$QuantumCircuitOperatorNames` is a PackageScope symbol, so my "not in names" check was a false negative; named `"Trotterization"[ops,order,steps,time]` is correct (verified order-1 err ~1/steps, order-2 ~1/steps^2). **Final cell (n=8):** `step = QuantumCircuitOperator["GroverPhase"[BooleanFunction[2^200, 8]]]`; full circuit `Join[Thread["H"->Range[8]], ConstantArray[step, kopt]]` with `kopt = Round[Pi/(4 ArcSin[Sqrt[1/256]]) - 1/2] = 12`; applied via `Method -> {"TensorNetwork", "Path" -> "Greedy"}`. Verified: finds the marked input `200 = 11001000`, success **0.99998** on the analytic peak $\sin^2((2k+1)\arcsin\sqrt{1/N})$; apply ~17 s (the "push"), `ContractionTree` cell ~0.6 s. **Honest framing (no false claims):** at n=8 dense is actually faster (TN overhead; the all-qubit diffusion is full-width so cost ~$2^{1.5n}$ and the path can't help much, TN does NOT beat dense here); the cell shows the default engine running a recognizable algorithm with the path steerable + the contraction tree inspectable, not a speed win. Memory `reference_grover_named_circuit_iteration_operator` written. Companion Grover usage doc came from a spun-off investigation session. Suite **95 cells** green, 0 messages; nb regenerated. |
| A95 | 1 (LZ) + 4 (stabilizer construction) + global (exactness) | Three user-requested edits made after A94 and verified green at the time, batched here for the log (the showcase had grown to 100 cells before the A96 interconversion split). (a) The Landau-Zener survival was still a compact block; user wanted it broken into cells with narrative. (b) The Claim 4 stabilizer-engine example built + applied + read the cluster in one folded cell, no diagram. (c) The document never named its numerical through-line: every deliverable is *exact*, the ground truth a sample converges toward, and that was left implicit. | Yes (user: "separate code cells, with proper narrative text" for LZ; "stabilizer example, break down code cells, and add proper text"; "address what u said as 'The one honest numeric strength'") | **(a) Landau-Zener survival split** into op / `survival[g,t]` / asymptotic-ratios / `Plot` cells with learning-by-computing bridges; legend keeps the `\.b2` escape for `²` (literal `²` mojibakes through `wolframscript -file`). **(b) Stabilizer-engine construction split** into three cells: `qc = QuantumCircuitOperator[Join[Thread["H"->Range[5]], {CZ chain}]]; qc["Diagram"]` (ordinary gate circuit) -> `ps = qc[Method -> "Stabilizer"]` (`PauliStabilizer`, $2^5$ vector avoided) -> `ps["PauliForm"]` $\{XZIII,ZXZII,IZXZI,IIZXZ,IIIZX\}=K_v$; narrative makes the engine-at-apply-time choice explicit, then the downstream expectation cell reuses the same `ps`. **(c) Exactness-as-ground-truth thread** named at the opening ("not estimates that tighten with more shots or a finer grid; they are *exact*") and synthesized at the close ("the quiet numerical claim under the symbolic one: where a numeric simulation or a finite measurement hands you an estimate, QF hands you the exact object it estimates"). Chosen over a speed claim deliberately: the audit shows dense statevector is ~130-320x slower than Aer, so the honest numeric strength is *exactness as ground truth*, not throughput. All verified green (run35, **100 cells**, 0 messages) before the A96 split took it to 102. |
| A96 | 4 (interconversion) | The "representations convert exactly" subsection packed the circuit build, the stabilizer apply, and the tableau expansion into a single 3-cell flow (`ps = QuantumCircuitOperator[...][Method -> "Stabilizer"]` folded the build and apply together, no diagram), and the `===` check rebuilt the whole `QuantumCircuitOperator[...]` a second time instead of reusing a named handle. | No (user supplied a 5-cell breakdown + a prose tweak) | **Interconversion subsection broken into 5 learning-by-computing cells.** (1) `qc = QuantumCircuitOperator[{"H" -> Range[3], "CZ", "CZ" -> {2, 3}}]; qc["Diagram"]` (the build now uses the short form, bare `"CZ"` riding the default first pair, and *shows the diagram*: an ordinary gate circuit, nothing stabilizer-specific yet). (2) `ps = qc[Method -> "Stabilizer"]` as its own cell (the `PauliStabilizer` object displayed). (3) `QuantumState[ps] // TraditionalForm` (tableau -> exact ket). (4) `QuantumState[ps]["StateVector"] === qc[]["StateVector"]` (the check now *reuses* `qc[]` instead of rebuilding the circuit). (5) `ps["Circuit"]["Diagram"]` (tableau -> 5-gate Clifford synthesis). Verified all five standalone, 0 messages: diagram `Graphics`, `ps` a `PauliStabilizer`, `QuantumState[ps]` $=\tfrac1{\sqrt8}\sum_x(-1)^{x_1x_2+x_2x_3}|x\rangle$ (statevector signs negative *exactly* on $011$ and $110$, matching the two CZ bonds, added to the prose as an earned detail), `===` $\to$ `True`, synthesis 5 gates. **Prose tweak (user):** "check it with `===`, the language's structural identity" $\to$ "check it with `SameQ`" (names the function; the code cell keeps the `===` operator, so the reader sees `===` *is* `SameQ`). Net +2 cells (Claim 4 interconversion 3$\to$5); `qc` is reused-then-reassigned downstream (CHSH, IBM), no leak. Verified in document order (cells 73-77): `qc["Diagram"]` Graphics, `ps` PauliStabilizer (3-generator tableau), ket boxes carry $\tfrac1{2\sqrt2}=\tfrac1{\sqrt8}$, `===` `True`, synthesis Graphics. Suite **102 cells** green, 0 messages; nb regenerated (315 KB, `SameQ`/structural-identity prose present). |
| A97 | 7 (OpenQASM import) | The CHSH-import example used **decimal** angles (`U(1.5707963267948966, 0., 3.141592653589793)`) and `NProbability`, so the winning probability came back as the float `0.8536` (then matched to $\cos^2(\pi/8)$ in prose only). It under-sold the import: a numeric answer where an exact one was available. | Yes (user supplied a symbolic-`Pi` QASM string + `Probability // FullSimplify`; asked to comment that "QF understands `U(Pi/2, 0, Pi)`, qiskit does not", and to verify it) | **Import example made exact.** Circuit angles switched to symbolic `Pi/2`, `Pi`, `Pi/4` (QF's importer evaluates each gate angle as a WL expression, so `Pi` is the exact constant and the imported object never touches floating point). Probability cell switched `NProbability[...]` $\to$ `Probability[BitAnd[x,y]==BitXor[a,b], {a,x,y,b} \[Distributed] qc[]["MultivariateDistribution"]] // FullSimplify`, which returns the **closed form** $\tfrac{2+\sqrt2}{4}=\cos^2(\pi/8)\approx0.8536$ (verified `== (2+Sqrt[2])/4` and `== Cos[Pi/8]^2` both `True`; 15-gate parse, 0 messages). **`Pi`-vs-`pi` claim verified honestly** in the `/tmp/qcbench` venv (qiskit 2.4.1 + `qiskit_qasm3_import`): a lone `U(Pi/2, 0, Pi) q[0];` raises `QASM3ImporterError: Undefined symbol 'Pi'`, while lowercase `pi` parses, so QF reading the capital `Pi` (= Wolfram $\pi$) is a real, demonstrable difference. **Scope guard (kept the doc honest):** qiskit *also* rejects QF's `ctrl(0) @ negctrl(1) @ U(...)` modifier syntax at the grammar level (`ParseCancellationException`), independent of `Pi`, so the **full** circuit would not round-trip into qiskit even lowercased; I therefore restricted the comparison strictly to the verified `U(Pi/2, 0, Pi)` token and did **not** claim the whole circuit parses after lowercasing. Added the user's code comment (`(* QF reads U(Pi/2, 0, Pi)... qiskit knows only lowercase pi *)`) and rewrote the narrative: import bridge now flags symbolic angles, a standalone sentence states the verified qiskit rejection, the result paragraph leads with the exact closed form ("the algebraic number a sampled estimate only converges toward"). No cell-count change (102, 1:1 swap). Verified in document order: cell094 (import) `Null`/clean, cell095 (`Probability // FullSimplify`) $\to$ `(2 + Sqrt[2])/4`. Suite **102 cells** green, 0 messages; nb regenerated (symbolic `Pi` ×4, exact-result prose, qiskit point in narrative). **nb caveat:** MarkdownToNotebook strips WL `(* *)` code-cell comments, so the `Pi`/qiskit comment lives in the MD only; the same point survives in the nb via the narrative sentence. |
| A98 | 3 (un-measurement) | The "Run the measurement backwards" opener leaned entirely on the isometry formalism ($V^\dagger V = I$, "exactly as von Neumann wrote it down"), so it read as a purely mathematical feature of QF rather than physical, realizable reversal. | Yes (user: the opening paragraph reads like a theory-only thing; include the experimental counterpart, concise + precise) | **Two sentences added to the opening paragraph** grounding the reversibility as laboratory physics, not just algebra: a measurement is reversible "in the laboratory, not only on paper", and that a measurement can be undone *while its record stays coherent, before it irreversibly decoheres* is an experimental fact, the reversal (or "uncollapsing") of a partial (weak) measurement having been demonstrated on superconducting qubits (Korotkov-Jordan proposal 2006, Katz et al. 2008), returning the qubit to its pre-measurement state. **Scoped precisely:** the addition states the shared *principle* (reversible until the record decoheres) and offers the weak-measurement experiment as its realization, without claiming QF's projective, coherently-recorded example *is* that specific experiment. The closing "reversible physics, $V^\dagger V = I$" sentence now reads as grounded rather than theoretical; left unchanged for concision. Prose-only: the four code cells in the subsection are byte-identical (un-measure still returns $\{\cos\tfrac\theta2, e^{i\phi}\sin\tfrac\theta2\}$), so no suite re-run; nb regenerated (101-line offset, "uncollapsing" prose present). |
| A99 | all (code hygiene) | Many cells still bundled several statements (master-equation setup, SIC POVM, coherent-state, spin-1, tomography, the 7-statement Grover, the 5-statement un-measure, ...), and several variables collided across self-contained examples (`qc` named **4 different** circuits, `ps` 2 stabilizers, `st` 2 states, `g` the QAOA graph vs the Landau-Zener adiabaticity) or used leading-uppercase against WL convention (`H`×4, `U`×3, `Hc`, `HLZ`, `ULZ`, `Jx/Jy/Jz`, `qcv`). | Yes (user: break every multi-statement cell to **one statement per cell** with concise lead-in; audit all variable names, distinct descriptive-lowercase notation for distinct objects, `psi`/`n` may stay shared, avoid leading uppercase. Two AskUserQuestion forks resolved: granularity = **strictly one statement per cell**; naming = **descriptive lowercase**) | **Whole-document split + rename pass.** **(1) Strictly one statement per cell:** every multi-object cell broken so each defined object gets its own cell with a concise bridge; build and display separated. Un-measure $\to$ 5 cells. **Cell count 102 $\to$ 166.** **(2) Descriptive-lowercase rename map:** `qc` $\to$ `cluster5`/`cluster3`/`chsh`/`ghz`; `ps` $\to$ `stab5`/`stab3`; `st` $\to$ `psit`/`coded`; `H` $\to$ `hheis`/`hzfs`/`hab`/`hdrive`; `Hc` $\to$ `hcost`; `HLZ`/`ULZ`/`matULZ` $\to$ `hlz`/`ulz`/`lzmat`; `U` $\to$ `urot`/`uosc`/`uexp`/`utgt`; `Jx/Jy/Jz` $\to$ `jx/jy/jz`; `qcv` $\to$ `qpe`; QAOA graph `g` $\to$ `graph` (the LZ adiabaticity ratio `g` kept, which resolves the collision); `psi` and `n` left as shared generics (the state / the system size) per the rule; the lone prose placeholder `QuantumQASM[qc, "Version"->2]` $\to$ `circuit`. Backtick prose refs updated alongside the code. **(3) Camel-case with lowercase lead kept** (`oneLayer`, `aOp`, `alphaKet`, `sicQubit1`, `mxZ`): "no uppercase" read as **no leading uppercase** (the WL builtin convention), internal caps idiomatic. **Verification:** grep confirms zero leftover `qc`/`ps`/`Hc`/`HLZ`/`ULZ`/`matULZ`/`qcv`/`Jx` and no bare `H =`/`U =`; full suite **166 cells green, 0 messages**, every key output unchanged (Rabi formula, `(2+Sqrt[2])/4`, stab generators `{XZIII,...}`, `===` `True`, IBM `Queued`/`QuantumMeasurement`); IBM cells re-stubbed at new positions (cell161 submit $\to$ `<|"Status"->"Queued"|>`, cell164 refresh $\to$ `ghz[]`). nb regenerated (349 KB). One slip caught + fixed mid-pass: an accidental edit to the unchanged JX-expectation cell added a stray code fence, removed immediately. |
| A100 | 4, 6, 7 (display polish + exact decimal) | After the A99 split/rename, a batch of user-supplied display refinements plus one exactness addition. | Yes (user supplied each code form across several turns) | **Display-options + exact-decimal pass (consolidated).** **(a) Grover success, exact then decimal:** `Max[found["Probabilities"]]` returns an **exact rational** (a ratio of ~46-digit integers; the TN contraction stays exact), so a `% // N` cell was added after it (user request) reading it as `0.9999470421032737`; prose reframed ("comes back as an exact rational, a ratio of large integers... Read it as a decimal"; "sitting **on** the analytic Grover peak", dropping the now-imprecise "exactly on"). **`%` forced a driver upgrade:** `%`/`Out` is a front-end feature absent in `wolframscript -file` (verified: bare `%` returns `Out[0]`), so driver2.wls now precomputes `lineForPct = $Line - 1` before the Scan and runs `Unprotect[Out]; Out[lineForPct] = res; Protect[Out]` after each cell, so `%` resolves to the previous cell's result (verified: cell112 rational $\to$ cell113 `0.99994704`). **(b) `uexp` matrix** wrapped in `MatrixForm@` for a $2\times2$ grid. **(c) AmplitudesChart** (QFT-of-cat) gains `AspectRatio -> 1/3`, `PlotRange -> {Automatic, 1}`. **(d) Bernstein-Vazirani ProbabilitiesPlot** gains `"LabelsAngle" -> 90 Degree`, `AspectRatio -> 1/3` (vertical bitstring labels, flatter panel). **(e) chsh `Pi`/qiskit note moved from a code comment to a text cell** right after the import, so it now survives into the nb (the converter strips code comments); the duplicate qiskit sentence dropped from the following paragraph. **(f) Grover `ContractionTree`** switched to `TreeElementLabel -> (All -> None)`, `TreeElementShapeFunction -> (All -> None)` (bare contraction-tree structure, no per-node labels/shapes); bridge + interpretation rewritten to describe the schedule's branching and depth rather than dimension labels. Cell count **166 $\to$ 167**. Full suite **167 cells green, 0 messages**; every changed output verified (uexp `{e^{-iXt}, MatrixForm}`, AmplitudesChart `Legended`, BV `Graphics`, `% // N` `0.99994704`, ContractionTree `Tree`); IBM re-stubbed at cell162/cell165. nb regenerated (the `% // N` cell stored as `RowBox[{"%", ...}]`, evaluates in the FE). |
| A101 | 4 (Grover) | After A100, the Grover default-engine section had no picture of the search circuit, and the `found` apply spelled out the $|0\dots0\rangle$ input explicitly. | Yes (user supplied both code forms) | **Grover diagram added + apply simplified.** (a) New cell `grover["Flatten"]["Diagram", "ShowGateLabels" -> False]` after the `grover` definition draws the flattened search (Hadamard layer + 12 diffusion blocks) with gate labels off so the repeated structure stays legible (`Graphics`, verified). (b) `found = grover[QuantumState[StringJoin[ConstantArray["0", n]]], Method -> ...]` $\to$ `found = grover[Method -> {"TensorNetwork", "Path" -> "Greedy"}]`: no explicit input, since the circuit's default initial state **is** $|0\dots0\rangle$ (verified identical, output an 8-qubit `QuantumState`, `Max` the same exact rational, top bitstring `11001000` = 200); dropped the `;` so the output state displays. The `% // N` chain is unaffected (`%` still reads the immediately-prior `Max` cell). Cell count **167 $\to$ 168**. Full suite **168 cells green, 0 messages** (cell111 diagram `Graphics`, cell112 found `QuantumState`, cell113 exact rational, cell114 `0.99994704`; IBM re-stubbed cell163/cell166). nb regenerated. |
| A102 | all (prose) + 5 + setup | (a) the state-calculus cell printed a SparseArray zero; (b) Setup loaded the installed paclet, not the dev build; (c) **numeric output values** quoted in prose (timings, decimals, sampled/structural counts) drift run-to-run or under a code revision. | Yes (user: add `// Normal`; new Setup with dev-paclet `PacletInstall`; then, scope clarified after a dismissed question, **remove only NUMERICAL output values, keep symbolic**) | **(a)** `Simplify[D[psi,{t,2}]["StateVector"] + psi["StateVector"]]` $\to$ `... // Normal` (zero vector shows as `{0, 0}`; verified standalone, 0 msgs). **(b) Setup block** now `PacletInstall["https://www.wolfr.am/DevWQCF", ForceVersionInstall -> True]` + `PacletInstall["https://www.wolfr.am/DevWTN", ...]` ahead of the three `Needs` (the user's backtick-stripped contexts restored to ``Wolfram`QuantumFramework` `` / `` `...`SecondQuantization` `` / `` `Wolfram`TensorNetworks` ``); bridge notes it installs the development builds. The dev install is **not** run in the offline harness (driver uses `PacletDirectoryLoad` on the working tree). **(c) Numeric-value purge** (numerical only; symbolic kept): stabilizer timing (`0.06 s in this run`, `three microseconds`, `a few hundredths of a second`) $\to$ "almost instantly"; Grover success `~0.9999` $\to$ "all but certain"; Wigner `+0.318`/`-0.236` $\to$ "positive/negative" (kept symbolic ceiling $1/\pi$); Husimi `~0.012` + Wigner `0.318` $\to$ "next to nothing"/"sharp peak"; Bloch length `~0.87` $\to$ "less than $1$"; CHSH `~0.8536` removed (kept $\tfrac{2+\sqrt2}{4}=\cos^2(\pi/8)$); QAOA optimum `~3.70` (×2) removed (kept "a root of a cubic"); tomography fidelity "a part in a thousand" $\to$ "very close to unity"; QPU "a few percent each in this run" removed; QPE TN "twenty-one vertices/hyperedges" $\to$ "vertices"/"one hyperedge per tensor"; stabilizer entropy "$500$-ebit ceiling" $\to$ "half-chain maximum". **KEPT** every symbolic/exact result (the "returns exactly as $...$" closed forms, $T_2\le2T_1$, `True`, exact $1$, the $g^{(2)}$ classification $0/1/2$, exact Pauli generators, input parameters $256$/$2000$/$4096$/$k{=}12$/marked $200$, exact combinatorial cut $4$ of $6$). Prose-only + one verified terminal cell + a loads block: no full suite re-run, cell count unchanged at **168**. nb regenerated (Setup `PacletInstall` present, 0 em-dashes). |
| A103 | 7 (hardware) | The hardware section assumed a connected IBM account ("With an IBM account connected through `ServiceConnect[...]`") but never showed the authentication step. | Yes (user supplied the auth code + asked for a sentence on creating the API key / CRN) | **Authentication cell added** before the GHZ submission: `api = "xxx"; LocalSymbol["ibm_crn"] = "xxx"; ServiceConnect["IBMQuantumPlatform", "New", Authentication -> {"apikey" -> api}]`, with a sentence telling the reader to create an API key and copy the instance cloud resource name (CRN) from their IBM Quantum Platform account. The `"xxx"` are **placeholders, no real credentials stored** (per the standing rule; real-credential scan clean). Intro reworded to flow into it; "The call returns a `ServiceObject` connection handle" added after. **Kept as one auth-setup block** (not split per one-statement-per-cell, mirroring the end Setup block, since it is credential setup). Like the `IBMJobSubmit`/`Refresh` cells it is illustrative and **not executed** in the offline harness (placeholder creds + account-gated); verified by `SyntaxQ` (`True`). Cell count **168 $\to$ 169** (the +1 sits in Claim 7, after the Grover `%` chain, so only the IBM cells shift; a future suite run must stub the auth cell too). No full re-run: the other 168 cells are unchanged from run40 (green), `// Normal` (A102) was verified standalone, the numeric purge was prose-only. nb regenerated. |
| A104 | all (display) | Many object-building cells were silent (`var = QuantumState/Operator/Circuit/...[...];`) after the A99 one-statement split, so the reader never saw the object's summary box until a later property cell. | Yes (user: whenever a cell builds an object with a summary box, e.g. `target = QiskitTarget[...]`, drop the `;` so the box shows on evaluation; use everywhere) | **Document-wide `;`-drop for summary-box objects** via a whitelist script (`dropsemi.py`): **53 cells** lost their trailing `;` so the object displays. Whitelist (LHS): `hlz`/`ulz`/`op`/`hcost`/`stateprep`/`qcost`/`mixer`/`oneLayer`/`meanvalue`/`hheis`/`psit`/`\[Sigma]m`/`\[Rho]0`/`\[Rho]t`/`psi`(×5)/`sic`/`sicQubit1`/`circ`/`aOp`/`alphaKet`/`uosc`/`wigner`/`husimi`/`mx`/`mxZ`/`{jx,jy,jz}`/`hzfs`/`true`/`est`/`tele`/`out`/`enc`/`qec`/`coded`/`backward`/`step`/`grover`/`net`/`cluster5`/`cluster3`/`uexp`/`hab`/`utgt`/`hdrive`/`qpe`/`chsh`/`target`/`ghz`/`job`. **KEPT `;`** on non-summary-box cells: scalars (`n`/`marked`/`kopt`), matrices (`ring`/`id2`/`zz`), lists (`ratios`/`gates`), the `oracle` `BooleanFunction`, scalar formulas (`amp`/`energy`/`crit`), `$Assumptions`, `SeedRandom`/`ClearAll`, function defs (`survival`/`syn`, `:=`), the `big` `AbsoluteTiming` wrapper, the Setup `Needs`, the auth `api`/`ibm_crn` strings. **Borderline objects included** (verified to display a box, message-free): `wigner`/`husimi` `InterpolatingFunction`, `net` `TensorNetwork`, `est` `QuantumStateEstimation`. Full suite **169 cells green, 0 messages** (the 53 now print summary boxes: `QuantumOperator`/`QuantumState`/`QuantumCircuitOperator`/`InterpolatingFunction`/`QuantumStateEstimation`/`TensorNetwork`/`QiskitTarget`, all clean); 4 cells stubbed for the offline harness this run (auth `ServiceConnect` cell162, `IBMJobSubmit` cell164, `Refresh` cell167, Setup `PacletInstall` cell169). No cell-count change (169). nb regenerated. |
| A105 | opening (education) | The opening enumerated seven technical features but said nothing about QF's value for teaching and learning, which the symbolic + numeric + visualization combination makes a genuine strength. | Yes (user supplied a draft + the book / Wolfram U links; asked to revise, cross-check, find best wording) | **Education paragraph added** at the end of the opening, **between the seven-claim list and the "we begin with the algebra" lead-in** (so the Claim 1 transition stays last). **Cross-checked the resources:** the book is *Quantum Computing by Model and Simulation: A Computational Approach* (Mohammad Bahrami, Wolfram Media, Summer 2026), a computation-first text built on the framework, matching this document's method; Wolfram U genuinely hosts quantum courses (Intro to Quantum Computing in WL, the QuantumFramework study group WSG31, Quantum Optics & Second Quantization WSG64), so "include quantum computing" is accurate (the product page itself 403s to automated fetch; confirmed via search + the user's pasted book copy). **Wording moderated** from the user's "one of the most useful tools" to "a particularly effective tool for teaching and learning quantum theory at every level, from a first qubit to graduate research" (confident, no superlative hype, matches the doc register); ties the book's computation-first philosophy to the showcase ("build a state, evolve it, draw it, and read its closed form in a single expression"). Author not named in the showcase (neutral voice); book cited by title + publisher + link. Two markdown links verified to convert to **nb hyperlinks** (no raw `](http` leak). Prose-only, no code; **169 blocks unchanged**, 0 dashes; nb regenerated. **Repositioned twice, same session (user flagged the spot):** the paragraph split the seven-claim list from its own "claims build on one another" summary, so it left the opening (a resources/"further reading" pointer lands better after the evidence than as a front-loaded pitch; "Taken together, these features" reads as an earned conclusion). First moved into *Where This Leaves Us* right after "The document re-derives itself"; then, on the user's "put it properly at the end", moved to the **genuine final paragraph**, after the Setup appendix and provenance footer. That ordering keeps "The document re-derives itself" as the clean close of the closing narrative and makes the education note the document's last send-off. nb re-regenerated (links still convert, no raw-link leak). |
| A84 | 2 (POVM) | The POVM-in-circuit example (A77) wrote gates with explicit placements (`"H"->1`, `QuantumOperator["RY"[1.05],{2}]`, ...). | No (user supplied a tighter form) | **POVM circuit tightened to default placements:** `{"H", "RY"[1.05] -> 2, "CNOT", "RZ"[2.04], sicQubit1}` (H@1, CNOT@{1,2}, RZ@1 by default; RZ angle 0.7->2.04). Verified: same structure, 4 SIC probs summing to 1, `ProbabilitiesPlot` renders. The `RZ` is local on qubit 1 so the reduced purity (0.876) and **$\|\hat r\|\approx0.87$ are unchanged** (purity invariant under local unitaries) -> the "length $\approx0.87$" prose stays exactly valid; the RZ only rotates which SIC outcomes are suppressed/enhanced. Suite **93 cells** green, 0 messages. |
| A80 | 2 (phase space) | The "A phase-space quasi-probability" section showed only the cat-state Wigner (one Plot3D). | No (user: audit Husimi in QF SecondQuantization; include it; show Wigner and Husimi side by side) | **Husimi-Q added beside the Wigner.** Audited `HusimiQRepresentation` (`SecondQuantization.m:473`, same signature as `WignerRepresentation`: `[state, {xmin,xmax}, {pmin,pmax}, "GridSize"/"GaussianScaling"]`, returns an `InterpolatingFunction`; loaded by the doc's Setup). Section retitled "Phase-space quasi-probabilities: Wigner and Husimi". Added a Husimi readout cell (`husimi[0,0]` -> $\approx0.012$, against the Wigner's $0.318$ at the origin: the Gaussian smoothing cancels the interference peak) and **replaced the single Wigner `Plot3D` with a `GraphicsRow` of the two surfaces side by side**. Verified contrast (the pedagogical payload): Wigner ranges $[-0.232, +0.318]$ (negative interference fringes = nonclassicality), Husimi is **non-negative everywhere** $[\sim0, 0.159]$ (smoothed Wigner, lobes at $\pm2\sqrt2$ each $\approx1/2\pi$, fringes washed flat); prose lands "only the Wigner can go negative, so Wigner negativity, not the always-positive Husimi, witnesses nonclassicality." Variables renamed `w`/`q` -> `wigner`/`husimi` (the single-letter `q` false-positive-collided with the `qubit[4] q` register name inside the Claim 7 OpenQASM string; leak-scan now CLEAN). Suite **93 cells** green, 0 messages. |
| A79 | 5 (+ 1) | Claim 5 ("a quantum object is an ordinary WL expression") was the weakest: property dispatch (Claim 6's thesis), a BORING `PauliDecompose -> Association` (user-flagged "so what"), a numeric `Plot` wrapper (object never enters Plot, only a number), and `Reduce`. None showed a WL function TRANSFORMING the object. | No (user: those cases are boring; the H-recovery example fits Claim 5 better; audit the kernel for how objects interact with WL functions e.g. `Exp[QO]`; opine then implement) | **Claim 5 rewritten around the verb upgrade "native functions operate ON the object."** A 5-agent kernel-interop audit found `QuantumOperator` overloads (UpValues, `QuantumOperator.m`): `Plus`:467, `MatrixExp`:503, `D`:562, `Tr`:578, `Commutator`:583, `Equal`:603, + a `NumericFunction` catch-all:493 lifting `Exp`/`Power`/`Sin`... New 8-cell set (all verified, self-contained, no global `$Assumptions`): **A** `(|0>+|1>)/Sqrt2 == |+>` -> True (arithmetic builds the state); **B** `MatrixExp[(-I t) QO["Z"]]` -> propagator object $e^{-iZt}$ = diag($e^{-it},e^{it}$) (`Exp[...]` identical); **C** `a X + b Z` Hamiltonian + `["PauliDecompose"]` round-trip `<|X->a,Z->b|>` + `Commutator[X,Y]==2I Z` True (replaces the boring Association); **D** the **H-recovery flagship MOVED from Claim 1**: `H = I D[U, t] @ U["Dagger"]` -> `<|X->Cos[t^2]/2, Y->Sin[t^2]/2, Z->t, I->0|>` (`D` acts on the operator, returns an operator); **E** state calculus `D[psi,{t,2}]+psi` -> 0 (harmonic identity); **F** solvers `Maximize[<X+Z>]` -> {Sqrt2, t->Pi/4} + the kept `Reduce` Werner threshold $0<p<1/2$. **Cut:** bell property-dispatch (overlaps Claim 6), the H2 Association, the numeric `Plot` wrapper. **H-recovery move verdict:** Claim 1 stays strong (still Rabi/LZ/QAOA/Heisenberg = 5-6 dynamics results, tighter as "symbolic-first time evolution"); roadmap bullet 1 + closing recap reworded (H-recovery clause moved to the Claim 5 sentence); cell D prose cross-refs "Claim 1 solved forward". **Honesty (audit-found, kept out):** `Integrate` on an operator is SILENTLY WRONG, `Series` doesn't truly expand, `/. t->val` doesn't substitute named gates, and the `NProbability`/distribution interop returns unevaluated (categories are symbolic `QuditName`); key-feature prose states the overload set is "real but finite" (`Eigenvalues`/`Det` via `obj["Matrix"]`). Suite **92 cells** green, 0 messages, leaks CLEAN. |
| A78 | 1 (Landau-Zener) | The LZ section ended on the special-function propagator matrix + the asymptotic scalar $e^{-\pi\Delta^2/2v}$, with no visualization. | Added (user, after a 5-candidate evaluation workflow chose it and then endorsed "three cases together"): the time-resolved diabatic-survival sweep. | **Three-regime survival plot added** (1 cell). A 5-candidate viz workflow (time-resolved $P(t)$, Bloch trajectory, $\Delta^2/v$ universality map, enriched avoided-crossing, full LZSM interferometry) ranked the time-resolved survival #1: it is the only one where the figure's geometry IS the section's thesis. Cell reuses the section's exact symbolic propagator `ULZ` (10 `ParabolicCylinderD` + `Hypergeometric1F1`/`HermiteH`/`Gamma`), builds $U(t,-T)=U(t)U(-T)^{-1}$, and `ListLinePlot`s $|\langle0|\psi(t)\rangle|^2$ for $\Delta^2/v \in \{0.1, 0.6, 1.6\}$ (diabatic/intermediate/adiabatic) with the asymptotic $e^{-\pi\Delta^2/2v}$ as dashed `Epilog` lines. The solid curves plunge through the crossing then RING (Landau-Zener-Stückelberg oscillations = the visible fingerprint of the parabolic-cylinder functions) around the dashed asymptotes: "the asymptotic formula is the long-time average of the exact answer." Verified: curves start at 1, plateau-track $\{0.855, 0.390, 0.081\}$ with finite-$T$ ringing, cross-checked vs raw `NDSolve` to $2\times10^{-7}$, norm to $10^{-14}$. Cost ~16 s (300-pt grid x 3, comparable to the LZ `FullSimplify` cell); $T\le10$ + dense sampling to avoid aliasing the shrinking-period ringing. Rejected LZSM interferometry (scope change: needs a driven Hamiltonian, abandons the closed-form propagator). Suite **92 cells** green, 0 messages, leak-scan CLEAN. |
| A77 | 2 (POVM) | The POVM section was a symbolic-only Born-rule derivation ($p_i = \tfrac14(1+\hat r\cdot\hat n_i)$ on a pure Bloch qubit). | No (user: also add a NONTRIVIAL quantum circuit that HAS a POVM as its measurement and compute/plot probabilities from the circuit; numeric OK) | **POVM-in-circuit example added** after the IC punchline (2 cells). Probed the API: a `QuantumMeasurementOperator["TetrahedronSICPOVM", {1}]` is a first-class circuit element (arity 1, targets qubit 1); a circuit ending in it returns a `QuantumMeasurement` with 4 SIC outcomes; **`ProbabilitiesPlot` works on the NUMERIC POVM measurement** (fails on exact-symbolic gates, which also give ugly dilation-normalized radicals, so numeric gates are used per the user's allowance). Example (nontrivial, entangling): `{"H"->1, RY[1.05]@2, "CNOT"->{1,2}, RZ[0.7]@1, sicQubit1}` -> diagram (POVM box on wire 1) + `circ[]["ProbabilitiesPlot"]`. **Illuminating twist:** qubit 1 is entangled with qubit 2, so its reduced state is MIXED (purity 0.876, $\|\hat r\|\approx0.87<1$, verified), and the 4 SIC probabilities $\{0.25, 0.058, 0.286, 0.406\}$ (deterministic Born-rule, A72-quotable; cross-checked === Born rule on the reduced $\rho_1$, diff $\{0,0,0,0\}$) are pulled in from the pure-state extremes toward uniform $\tfrac14$ -- extends the IC/tomography theme to mixed, entangled qubits. Self-contained (new `sicQubit1`/`circ` names, within the POVM section, leak re-scan CLEAN). Suite **91 cells** green, 0 messages. NB stale (regenerate later). |
| A76 | nb | The .nb was doubly stale (built at A73 on the 90-cell pre-restructure md; then A74 removed a section and A75 swapped 9 circuits) and still carried broken-bar kets. | Regenerated (user: "invoke latest md2nb, generate nb") | **`QF-Showcase.nb` rebuilt** with the now-bra/ket-fixed converter (`task_bc61c0f4` had landed `diracTemplatize` + the `texBoxes` hook in `MarkdownToNotebook.wl`; verified present before running). Command: `MarkdownToNotebook["...QF-Showcase.md", "...QF-Showcase.nb", "Evaluate" -> False]`. Result: 89 Input cells, 1 Title / 8 Section / 34 Subsection (one fewer than A73's 35: the "record is a wire" subsection removed at A74), 457 InlineFormula / 3 DisplayFormula, 0 `\!` leaks / 0 raw backslash-bang / 0 `$Failed`. **Bra/ket now correct: 86 `Ket`/`Bra`/`BraKet` TemplateBoxes and ZERO `AdjustmentBox[\[RightAngleBracket]/\[LeftAngleBracket], BoxMargins->{{∓0.2,...}}]` broken-bar boxes** (the defect from the user's manual-ket screenshot is gone). Code-cell fidelity re-confirmed: sampled multi-line cells (QFT[12], 5-qubit cluster) export back to exact source via FE `ExportPacket->"InputText"`. |
| A75 | 4, 6, 7 | GHZ was reused in ~8 forms across the engines/viz/interop claims (GHZ[10], GHZ[3]×several, GHZ+Fourier, GHZ measure, PauliStabilizer["GHZ"]). | No (user: stop reusing GHZ; each example a different NAMED circuit where possible; audit named circuits, pick good cases e.g. Grover/QPE; for method/advanced examples use more complex cases, not few-qubit toys) | **Circuit-variety pass via a 9-agent verification workflow** (each agent owned one GHZ site, picked + kernel-verified an advanced named circuit in role, veto power). Audited `$QuantumCircuitOperatorNames`-equivalent registry (`QuantumCircuitOperator/NamedCircuits.m`): Bell, BernsteinVazirani, CHSH, Deutsch(Jozsa), Fourier, Fredkin, GHZ, Graph, PhaseEstimation, Simon, Toffoli, Trotterization, oracles. **GHZ reduced from ~8 occurrences to ONE** (the frozen `ibm_fez` hardware run, which must stay GHZ). New map, all outputs re-verified: **(C4 TN path)** GHZ[10] -> **QFT[12]** (`"Fourier"[12]`): all $2^{12}=4096$ amplitudes share magnitude $1/64=2^{-6}$ (uniform superposition); richer all-to-all contraction than the 1D GHZ chain; ContractionTree on the greedy path renders. **(C4 stabilizer engine)** GHZ[3] -> **5-qubit linear cluster state** via explicit H+CZ (Method->"Stabilizer" works on H+CZ but NOT on `"Graph"[g]`); gens $\{XZIII,ZXZII,IZXZI,IIZXZ,IIIZX\}$ = $K_v = X_v\prod_{w\sim v}Z_w$; expectations $\langle K_1\rangle=\langle K_3\rangle=1$, $\langle XXIII\rangle=0$ (anticommutes $K_1$). **(C4 interconversion)** GHZ[3] -> **3-qubit linear cluster** (agent vetoed 4q for 3q clean ket): $\tfrac1{\sqrt8}\sum_x(-1)^{x_1x_2+x_2x_3}|x\rangle$, `===` vs default engine True, `ps["Circuit"]` 5-gate. **(C6 AmplitudesChart)** GHZ+QFT -> **QFT of cat** $\tfrac1{\sqrt2}(|000\rangle+|101\rangle)$: interference comb + phase ramp, null at $|100\rangle$. **(C6 ProbabilitiesPlot)** GHZ measure -> **Bernstein-Vazirani**[{1,0,1,1}]: deterministic single spike at $1011$ (hidden-string recovery), far richer than 2 GHZ bars. **(C6 TableauForm)** PauliStabilizer["GHZ"] -> **`PauliStabilizer[QuantumCircuitOperator["Graph"[CycleGraph[5]]]]`** 5-ring cluster: 10×10 tableau whose $Z$ block IS the $C_5$ adjacency matrix. **(C6 Diagram+TN)** GHZ+QFT -> **QPE**[3] (`"PhaseEstimation"[3]`, the subroutine in Shor/HHL): 21-vertex TN graph, 21 hyperedges. **(C7 QASM export)** GHZ[3] -> **Toffoli**: emits the Clifford+T decomposition (6 CNOTs + T/T† + 2 H), not a black-box ccx. **(C7 transpile)** GHZ[3] -> **QFT[3]** to the device target: controlled-phases decomposed to rz/sx/x + cx routed on the coupling map (qiskit-bridge cell, as before). Variety: QFT (×3 distinct roles), linear/ring cluster (×3 graphs), BV, QPE, Toffoli; only IBM keeps GHZ. All sections stay self-contained (A74): each cluster cell rebuilds its own circuit; identifiers `ghz`->`qft`/`ps`/`qcv` local, **leak re-scan CLEAN**. Suite **89 cells** green, 0 messages. NB doubly stale now (regenerate after the bra/ket spin-off). |
| A74 | 3, all | Claim 3 opened with a "The record is a wire" seed (GHZ+QFT, three measures, `"CNOT" -> {-1, 2}`); the doc reused `psi` across sections (defined in the Claim 2 channel cell, read by POVM, teleportation, un-measure) and `ps` across Claim 4 subsections. | No (user: the seed example's interest is unclear, remove it; make every section/example self-contained, no variable leak between others, e.g. `psi`) | **(1) Seed removed.** "The record is a wire" subsection (the `ff` diagram cell) deleted; its record-wire pedagogy (the $0/-1/-2$ pointer numbering) folded into **Teleportation**, now Claim 3's opener: the diagram prose names the `{1}`/`{2}` measurements dropping records onto wires $0$ and $-1$ and the corrections `"CNOT" -> {-1, 3}` / `"CZ" -> {0, 3}` as gates controlled on those record wires ("the phone call has become two wires"). Claim 3 = 3 subsections (Teleportation, coherent-error QEC, run-backwards). **(2) Self-containment.** A document-wide cross-section leak scan (segment by heading, flag any user var used before assignment in its own section) found exactly two real carriers: `psi` (the $\theta,\phi$ Bloch qubit) leaking from the Claim 2 channel cell into POVM + teleportation + un-measure, and `ps` leaking from Claim 4's stabilizer subsection into the interconversion subsection. Both re-defined locally: `psi = QuantumState[{Cos[\[Theta]/2], Exp[I \[Phi]] Sin[\[Theta]/2]}]` added to the POVM, teleport, and un-measure cells (prose drops "from the channel example"/"from Claim 2", states the closed form inline); `ps = QuantumCircuitOperator["GHZ"[3]][Method -> "Stabilizer"]` added to the first interconversion cell. The `t` hits were dummy/formal time + plot variables (false positives), confirmed. **Re-scan: zero remaining cross-section leaks.** Each `###` example now runs standalone given the Setup loads. Suite re-run **89 cells** green (was 90; one cell removed), 0 messages: POVM -> SIC Born probs, teleport -> $\{0,0,0\}$, un-measure -> $\{0,0\}$, interconversion ket -> GHZ TemplateBox, all unchanged. 0 em dashes. NB regeneration (A73) now stale again by one section. |
| A73 | nb | The .nb was stale (75 cells / 5 claims, last built before the A64-A72 md work). | Regenerated (user: "regenerate nb file") | **`QF-Showcase.nb` rebuilt from the final md** via `MarkdownToNotebook["...QF-Showcase.md", "...QF-Showcase.nb", "Evaluate" -> False]` (newest converter at `GitHub/MarkdownToNotebook/MarkdownToNotebook.wl`, vendored `Wolfram`Parser`` present). Structure verified: head `Notebook`; **90 Input** (= 90 md code cells), 1 Title, 8 Section (7 claims + closing), 35 Subsection, 162 Text, 446 InlineFormula, 3 DisplayFormula, 7 ItemNumbered; **0** literal `\!` negative-space leaks, **0** raw backslash-bang outside box headers, **0** `$Failed` parses. **Faithfulness check:** 86/90 cells parse under bare-kernel `MakeExpression`; the 4 that don't (46 ring, 52 teleport, 54 syndrome, 71 conc/Plot) are exactly the cells with a hard source line break immediately after an open `[`/`{` + reindent, which `ReparseBoxStructurePacket` stores as nested `"\n"`/indent string tokens. The FE (where the user opens the nb) parses, evaluates (cell 46 -> correct ring matrix), and **round-trips all 4 boxes back to the exact source code** (FE `ExportPacket -> "InputText"` === source modulo whitespace), so the code is character-faithful; bare-kernel `MakeExpression` is simply stricter than the FE about mid-bracket line breaks. No md changed. |
| A72 | all (prose) | Quoting sampled outputs verbatim (with A66's confirm-before-quoting rule) was sufficient for statistical results. | No (user: numbers produced by sampling must not be quoted at all, "since statistics can be different"; find a different way to express; apply everywhere, full read) | **Statistical-number purge over the full md.** Every sampling-derived value rewritten as a robust statement; deterministic numbers untouched. (1) Tomography counts ($1605/395$) -> "multinomially sampled tallies scattered around the Born-rule proportions, different on every fresh sample". (2) Recovered $\rho$ entries -> the TRUE entries stated exactly ($\rho_{11} = \tfrac34$, $\rho_{12} = \tfrac{\sqrt3}{4}e^{-i\pi/4}$) + "entry-wise residuals at the scale shot noise sets, $\sim 1/\sqrt{2000}$; which entries deviate ... changes with every fresh sample". (3) Fidelity $0.99998$ -> "within a part in a thousand of unity, and typically much closer" (infidelity $\lesssim 1/N_{\rm shots}$, seed-robust bound). (4) Stabilizer entropy $496$ -> "a few ebits shy of the $500$-ebit ceiling ... whatever gate sequence the sampler draws, the value is an exact integer from an $\mathbb F_2$ rank". (5) QPU bars $0.428/0.420/15\%$ -> "two tall bars land visibly short of $\tfrac12$, a few percent each in this run, with the missing weight spread thinly over the six forbidden bitstrings". (6) Closing recap: fidelity -> "a hair below unity"; $496$ ebits -> "near-maximal half-chain entanglement computed exactly". Survivors audited as deterministic: cat Wigner $\pm0.318/-0.236$, H$_2$ coefficients, CHSH $0.8536 = \cos^2(\pi/8)$ (exact identification), timing (run-stamped), $2000$/$4096$ (input parameters). Prose-only: extracted cells byte-identical to verified run-12 (90 cells); no re-run needed. Pipeline log itself keeps the captured values as the internal verification record. |
| A71 | 2 (optics) | The $g^{(2)}$ classification alone was the right content for "Continuous-variable quantum optics". | No (user: read ALL of `OngoingProjects/Courses/QuantumOptics/BookFormat` (8 chapter .nb, read 8/8 via nb-reader) and find a better example: coherent, nontrivial, symbolic preferred) | **Section rebuilt around the coherent state (from Book Ch. 2), $g^{(2)}$ kept as the closing.** Two new symbolic cells, both probed at HEAD: (1) eigenvalue equation $\hat a\|\alpha\rangle = \alpha\|\alpha\rangle$ via `Most[(aOp[alphaKet] - \[FormalAlpha] alphaKet)["AmplitudesList"]]` on the `"Normalized" -> False` coherent state -> 15 symbolic zeros (truncation edge excluded); (2) the most-classical-state identity $e^{-iHt}\|\alpha\rangle = e^{-i\omega t/2}\|\alpha e^{-i\omega t}\rangle$ under $H = \omega(\hat a^\dagger\hat a + \tfrac12)$, as a state equality `U @ CoherentState[] == E^(-I \[Omega] t/2) CoherentState[][\[FormalAlpha] E^(-I \[Omega] t)]` -> **True with $\alpha, \omega, t$ all symbolic** (2.6 s). Bridges carry the problem formulas per A68. **Trap caught:** the ladder operator must NOT be bound to `a`, which the CHSH `NProbability` cell uses as a `\[Distributed]` random variable; named `aOp`. Candidates considered from the book and passed over: JC collapse-revival (iconic but heavy/numeric), displacement composition (covered conceptually by the identity), squeezed-vacuum variances (numeric), no-jump decay $\|\alpha e^{-\gamma\delta t/2}\rangle$ (overlaps the master-equation section). Section flows into the cat-state Wigner section, whose $(\|\alpha\rangle + \|-\alpha\rangle)$ language is now pre-introduced. Suite re-run green (**90 cells**). |
| A70 | 1 | (follow-up to A69) The `Cases[..., _ParabolicCylinderD, ...][[1]]` extraction cell was the right display for the LZ propagator. | No (user: replace with `FullSimplify[Normal[ULZ["Matrix"]]]`, showing the whole matrix) | **LZ cell swapped to the full simplified matrix.** Probed first: `FullSimplify` takes $\approx 13$ s and the simplified matrix still carries all three families (`Hypergeometric1F1`, fractional-order `HermiteH`, `ParabolicCylinderD` plus `Gamma` glue; LeafCount 686); PCD instances have indices $\pm i\Delta^2/4v$ (and $1 - i\Delta^2/4v$) with arguments $(-1)^{1/4} t\sqrt v$ and $(-1)^{3/4} t\sqrt v$; entry $[1,1] = e^{-ivt^2/4}\,{}_1F_1(i\Delta^2/8v; 1/2; ivt^2/2)$. Bridge -> "simplify its full matrix"; outcome rebuilt on the verified facts: the $[1,1]$ entry quoted as typeset math (selective extraction per A63), cylinder indices/arguments stated exactly ($e^{i\pi/4}$ and $e^{3i\pi/4}$ rotations, NOT $e^{\pm i\pi/4}$), one-family-three-ways Weber framing, propagator checks and $P = e^{-\pi\Delta^2/2v}$ retained. Suite re-run green (88 cells). |
| A69 | 1 | The LZ outcome paragraph ("the whole propagator is built from these solutions of the Weber equation") fairly described the full $U(t)$ matrix. | No (user evaluated `Normal[ULZ["Matrix"]]` and saw the entries mix `Hypergeometric1F1`, fractional-order `HermiteH`, and `ParabolicCylinderD`; "the text needs major revision for this part") | **LZ outcome paragraph rewritten against the actual matrix.** The `Cases[...][[1]]` cell's captured output was and is `ParabolicCylinderD[-I \[CapitalDelta]^2/(4v), (-1)^(1/4) \[FormalT] Sqrt[v]]` (re-confirmed from `suite_run10.log`), so the extracted-function sentence sharpened: index $\nu = -i\Delta^2/4v$, argument $z = e^{i\pi/4}\sqrt v\,t$, purely imaginary order + complex-rotated time as the LZ hallmark. The dishonest-by-omission clause replaced with the truth about the full matrix: entries mix `Hypergeometric1F1`, fractional-order `HermiteH`, and `ParabolicCylinderD` with $\Gamma$-coefficients in $\Delta^2/v$, one function family written three ways (all Weber-equation solutions, interconvertible by standard identities: $D_\nu(z) = 2^{-\nu/2} e^{-z^2/4} H_\nu(z/\sqrt2)$, $D$/$H_\nu$ as ${}_1F_1$ combinations), "in the raw form the symbolic solver found it". Propagator checks ($U(0)=I$, SE, unitarity) and the $P = e^{-\pi\Delta^2/2v}$ asymptotics retained. Prose-only; cell unchanged and still verified. |
| A68 | all (prose) | Bridges that named the computation in words were sufficient problem statements. | No (user: before each example, a minimal closed-form formula of what is computed plus the motivation, so a reader with no WL/QF still understands the problem; "delicate, line by line") | **Problem-statement pass over all 88 cells, read line by line.** Added a minimal formula + motivation ONLY where missing (18 sites); already-equipped examples (Lindblad, Born rule, $g^{(2)}$, Wigner integral, $B^{-1}AB$, ZFS, Fattal, $i\dot U U^\dagger$, $\|\langle1\|U\|0\rangle\|^2$) left untouched. Additions: general qubit ket $\cos\alpha\|0\rangle + e^{i\beta}\sin\alpha\|1\rangle$; Bloch triple $(\langle X\rangle,\langle Y\rangle,\langle Z\rangle)$; rotating-field $H(t)$ display; $[H(t_1),H(t_2)]$ named; LZ $H(t) = \tfrac{vt}{2}Z + \tfrac\Delta2 X$ + stay-or-jump motivation; target trajectory $U(t) = R_Z(t^2)R_X(t)$; **MaxCut objective** $\max_s \sum \tfrac{1-s_is_j}{2}$; quench $\|\psi(t)\rangle = e^{-iHt}\|01\rangle$; $S(t) = -\operatorname{Tr}\rho_1\log_2\rho_1$; **amplitude-damping Kraus pair** ($K_0, K_1 = \sqrt\gamma\|0\rangle\langle1\|$, **kernel-verified** against `QuantumChannel["AmplitudeDamping"]` on a symbolic matrix, zero difference) + purity-cost question; ring $H = -\sum(\|j\rangle\langle j{+}1\|+\mathrm{h.c.})$; **tomography inversion** $\rho = \tfrac12(I + \sum\langle\sigma\rangle\sigma)$; teleportation success criterion $\rho_{\rm Bob} = \|\psi\rangle\langle\psi\|$; QEC theorem-to-check ($F = 1$ vs $\alpha\|000\rangle+\beta\|111\rangle$); stabilizer group $\{P : P\|\psi\rangle = +\|\psi\rangle\}$; tableau-to-state as solving $S_i\|\psi\rangle = \|\psi\rangle$; **Werner closed form** $\rho_W(p) = (1-p)\|\Psi^-\rangle\langle\Psi^-\| + \tfrac p3(I - \|\Psi^-\rangle\langle\Psi^-\|)$ (**kernel-verified**: QF's `"Werner"[p,2]` matches this singlet convention exactly, NOT the $p\,\rho_\Psi + (1-p)I/4$ textbook variant); realignment positive-iff-entangled gloss; $\psi_x = \langle x\|\psi\rangle$ for AmplitudesChart; Born $p(x) = \|\langle x\|\psi\rangle\|^2$ for the histogram; **CHSH classical bound** $\tfrac34$ as motivation. Prose-only: extracted cells **byte-identical** to the verified run-10 set, so no suite re-run needed; 0 em dashes. |
| A67 | 2 | The five-site ring was fine as one cell (build matrix + Fourier diagonal together, matrix never shown). | No (user: split; show `ring // MatrixForm` first, then the Fourier-diagonal readout as its own cell) | **Ring section split into two cells with the site-basis picture made visible.** Cell 1: `n = 5; ring = -Table[Boole[...]]; ring // MatrixForm`, outcome names the circulant structure ($-1$ on the two neighbor diagonals and in the far corners, the ring-closing bond). Cell 2 (user's layout verbatim): `Normal @ Chop @ N @ Diagonal[QuantumOperator[QuantumOperator[ring], QuantumBasis["Fourier"[n]]]["Matrix"]]`. Bridge now states the Bloch-theorem expectation between the two cells (local hopping in the site basis -> diagonal in the momentum basis), so the section reads concept -> matrix -> rewrite -> dispersion. Dispersion outcome sentence unchanged. Suite re-run green (**88 cells**). |
| A66 | 2, 6, all | The Fock-1 Wigner example and its "Verified: $W(0,0) = -0.318 < 0$" line were robust, and the doc's other quoted result-numbers were all still accurate. | No (user supplied the cat-state `WignerRepresentation`/`Plot3D` pair and ordered a doc-wide sweep: every measured-result number must be removed or re-confirmed, since small code changes shift them) | **(1) Wigner section rebuilt on the cat state** `CatState[][2, 0]` ($\alpha = 2$, even cat): readout cell `{w[0, 0], w[0, 1/2]}` -> $\{+0.318, -0.236\}$, both **probed live** ($w(0,0) = 0.31824 \approx 1/\pi$, the Wigner ceiling, at the midpoint where a classical mixture has nothing; trough $-0.2356$ at $(0, \pm 0.5)$; lobes at $x = \pm 2\sqrt2$ each $0.1592 \approx 1/2\pi$, the half-weight coherent blobs); user's `Plot3D` over $\{-6,6\}\times\{-2,2\}$ verbatim (`Graphics3D`). Narrative: interference peak = the identification that matters, fringes = the coherence, decoherence erases exactly them. **(2) Number sweep over the whole doc** (prose decimals + spelled counts): confirmed against `suite_run8.log` and probes: tomography counts $\{1605, 395\}$ (seeded), recovered $\rho$ $0.7514 / 0.3018 - 0.3093i$, fidelity $0.99998$, H$_2$ decomposition $\{-0.4804, 0.3435, -0.4347, 0.5716, 0.091, 0.091\}$, CHSH $0.85355 = \cos^2(\pi/8)$, entropy 496, 13 hyperedges (= 3 states + 10 gates), 4-gate synthesis, $0.06$ s timing (guarded "in this run"), hardware $0.428/0.420/15\%$ (frozen live-job data). **One real error caught:** the viz claim's "eight bars with equal-magnitude structure" for QFT(GHZ) is FALSE; actual magnitudes $\{0.5, 0.462, 0.354, 0.191, 0, \ldots\}$ mirror a cosine envelope, $\|(1 + \omega^{7x})/4\| = \|\cos(7\pi x/8)\|/2$, with an exact zero at $\|100\rangle$; outcome sentence rewritten (cosine envelope, zero bar named). Suite re-run green (**87 cells**). |
| A65 | 1 | The QAOA section could go from the closed-form landscape straight to `Maximize` with no visual. | No (user supplied the `Plot3D` cell: full $2\pi\times2\pi$ window, `"TemperatureMap"`, `PlotPoints -> 50`; "add proper narrative text") | **Landscape surface cell added** between the closed form and `Maximize`. Bridge: "because the landscape is a formula, the whole optimization problem can be looked at before it is solved." Outcome sentence carries the verified physics: smooth and doubly $\pi$-periodic (every term enters through $2\gamma$/$2\theta$, so the $2\pi$ window tiles the unit cell four times), **flat at $\tfrac32$ along $\gamma = 0$** (cost layer off; the equal superposition cuts $\tfrac32$ of $3$ edges on average, and $R_X$ leaves $\|+\rangle^{\otimes 3}$ invariant up to phase, checked exactly), and the contrast that lands the claim: an experiment estimates this surface point by point with shot noise, here it is one exact object whose peaks the next cell certifies algebraically. `Maximize` bridge reworded to "Take the maximum exactly:". Verified in context: `Graphics3D`, message-free; grid-sampled range $[0, 1.9993]$ consistent with the exact maximum $2$. Suite re-run green (**87 cells**). |
| A64 | all (restructure) | The five-claim structure was final: stabilizer belonged in Claim 2's scope story, the TN contraction path in the interop claim, and measurement/feedforward had no claim of its own. | No (user: implement the `QF-Showcase-Claim6-Feedforward-Implementation.md` handoff after opining; the opined plan, dedicated mid-document feedforward claim + simultaneous engines elevation, approved as "very carefully implement and revise only md file") | **Restructured to seven claims (md only; the .nb is deliberately left stale this pass).** **New Claim 3 "Measurement records are quantum wires, and feedforward is a controlled gate"** (8 cells): record-as-wire seed (GHZ+QFT, three measurements, `"CNOT" -> {-1, 2}` off a record; diagram) -> deferred-measurement **teleportation without a classical channel** (`"CNOT" -> {-1, 3}`, `"CZ" -> {0, 3}`; Bloch-vector difference $\{0,0,0\}$ **symbolically** for Claim 2's $\psi(\theta,\phi)$, re-verified in that exact parametrization) -> **repetition code with a coherent $R_X(2\theta)$ error**: projector-list syndromes $(I \pm ZZ)/2$ via `QuantumMeasurementOperator[{P+, P-}, {q1, q2}]` (the rank-1 observable trap from the handoff avoided), mixed-polarity decode `"C"[X, ones, zeros]`, syndrome weights $\{\cos^2\theta, 0, 0, \sin^2\theta\}$ (**digitization of errors**, $\alpha,\beta$-independent), codeword fidelity **exactly 1** symbolically (vs the encoder-built codeword) -> **un-measurement** `m["SuperOperator"]["Dagger"]` ($\{0,0\}$ symbolic). Honesty paragraph de-comparativized per house rule (no platform framing); $2^{n+k}$ cost stated plainly and used as the segue to the engines claim. Magic-state injection skipped (opined: too much machinery for one more instance of the same mechanism); constant-depth/MBQC/Wigner's-friend stay deferred in the handoff doc. **New Claim 4 "One object model, three computational engines"**: TN-path apply + `ContractionTree` **moved out of the interop claim** (`psi` -> `ghz10` rename to protect Claim 3's use of Claim 2's `psi`), stabilizer ladder **moved out of Claim 2** with a new entry cell `QuantumCircuitOperator["GHZ"[3]][Method -> "Stabilizer"]` -> `PauliForm` $\{XXX, ZZI, IZZ\}$ (engine dispatch on the same object), and a new **interconversion section**: `QuantumState[ps] // TraditionalForm` (exact ket), `QuantumState[ps]["StateVector"] === qc[]["StateVector"]` -> `True` (structural identity, global phase included), `ps["Circuit"]["Diagram"]` (4-gate synthesis). Renumbered: WL-expression 3 -> 5, visualization 4 -> 6, interop 5 -> **7** (retitled "An interoperability hub, down to real hardware"; key feature now outward-only since the inward/engine half moved to Claim 4). Cross-refs fixed: QASM note "(Claims 1 and 3)" -> "(Claims 1 and 5)"; viz "Claim 5 steers" -> "Claim 4 steered"; "hardware result in Claim 5" -> Claim 7; tableau section "stabilizer states of Claim 2" -> Claim 4. Roadmap rebuilt as 7 items; closing recap rewritten (seven claims, feedforward + engines sentences added; reader handoff gains "tilt the repetition code's coherent error off the $X$ axis"). **Timing drift caught by the re-run:** the n=1000 `ApplyCircuit` cell now folds in **0.06 s** at HEAD (compiled fast path), so the stale "0.47 s in this run" prose was updated to the captured value. **Full 86-cell suite green in document order** (driver mirrors the 3-load Setup block; IBM cells 82/84/85 patched to never resubmit the paid job; teleport/QEC/un-measure/interconversion cells all verified live: $\{0,0,0\}$, $\{\cos^2\theta,0,0,\sin^2\theta\}$, $1$, $\{0,0\}$, `True`). 0 em dashes. |
| A63 | all | Outcome sentences restating the cell's literal output (lists/associations) were per-A46 house style. | Refined by user (no zero-value repetition; echo only when it adds value or rewrites differently, e.g. the $U(t)$ display) | **De-echo pass over all 75 outcome sentences.** Rewrote ten that merely reprinted what the evaluated notebook already shows: the ring diagonal list (-> "exactly the tight-binding dispersion at the five Bloch momenta"), the $g^{(2)}$ association (-> "zero for the single photon, one for the laser, two for thermal light"), the tomography counts list (-> selective reading of the $X$ tallies), the Bell-dispatch association (-> the four facts in prose), the stabilizer expectation association and generator list, the POVM north-pole rationals (-> "a half on the nearest vertex, a sixth on each of the rest"), the QAOA partition echo, the spin-1 $J_z$-superposition ket echo, and the Heisenberg evolved-ket echo (-> oscillation between $\|01\rangle$/$\|10\rangle$ at frequency $2J$, naming the physics instead). **Kept** the value-bearing repetitions: typeset formula rewrites (amplitudes/Bloch/Rabi/criterion/landscape), the two matrix displays, diag() compressions, identifications (Tsirelson), selective extractions (commutator diagonal), numbers woven into interpretation (496/500, 0.99998, 0.47 s), and the recovered-vs-true $\rho$ juxtaposition. Prose-only; no cells changed; memory rule updated with the refinement. |
| A62 | 2 | The POVM section's `<\|Outcomes -> 4, ...\|>` sanity check was an adequate example. | No (user: badly boring; do a real symbolic computation with symbolic measurement results) | **Fully rewritten as a symbolic Born-rule measurement.** The generic Bloch qubit $\psi(\theta,\phi)$ (reused from the channel section) measured against all four `TetrahedronSICPOVM` effects: `Tr[psi["DensityMatrix"] . Normal[#]] & /@ sic["POVMElements"]` -> the **closed forms $p_i = \tfrac14(1 + \hat r\cdot\hat n_i)$** ($\tfrac{1+\cos\theta}{4}$ and three $\tfrac1{12}(3-\cos\theta+\sqrt2\sin\theta(\dots))$ terms), summing to 1 identically; second cell `probs /. \[Theta] -> 0` collapses to $\{\tfrac12,\tfrac16,\tfrac16,\tfrac16\}$ (the old numeric example survives as a special case). Closes with the IC punchline: the four probabilities ARE the state (solvable back for $\theta,\phi$), the alphabet of tomography. **Route matters:** `sic[psi]["ProbabilitiesList"]` returns the dilated-normalized `Sqrt[(...)^2]`/`Abs` quotient form that neither `Simplify` nor `FullSimplify` untangles; the explicit Born rule over `["POVMElements"]` gives the clean polynomials AND matches the bridge formula pedagogically. The `{4,2,True}` check cell dropped (its facts folded into the bridge prose). Suite green (**75 cells**). |
| A61 | 1 | The QAOA section's unlabeled blocks, direct `landscape` build, and `NMaximize` were final. | No (user supplied the labeled/`Maximize` version) | **QAOA rebuilt per user's code.** (1) `"Label"`s on every block (`Hamiltonian`/`Prep`/`Cost`/`Mixer`), motivated in the narrative: the labels name the blocks in the new diagram. (2) **New cell**: `meanvalue = (oneLayer /* Hc /* oneLayer["Dagger"]); meanvalue["Diagram"]`, the bra-ket sandwich *drawn*, labeled layer, `Hamiltonian` block, mirrored daggered layer (verified `Graphics`); the bra-ket-as-one-object paragraph now lands on a picture instead of an assertion. (3) `landscape` reads from the named `meanvalue` object. (4) **`NMaximize` -> `Maximize`**: returns exactly `{2, {\[Theta] -> -5\[Pi] - ArcTan[1/Sqrt[2]], \[Gamma] -> ...Root[...]}}`, the maximum AND the optimal angles as closed-form algebraic numbers, which retires the A44-era wording compromise ("optimized directly, no sampling noise") in favor of the honest stronger claim: genuinely analytic optimization. Suite green (**74 cells**). **Follow-up (user: payoff cell as `st // TraditionalForm`):** applied with one refinement, `FullSimplify[st] // TraditionalForm`, because the raw `QuantumEvolve` amplitudes are stored in the exponential form $\tfrac12 e^{-iJt}(1+e^{4iJt})$ and would render ugly; `FullSimplify` threads through the `QuantumState` upvalue (verified) and the ket renders as $e^{iJt}\cos(2Jt)\,\|01\rangle - ie^{iJt}\sin(2Jt)\,\|10\rangle$. Outcome sentence matched to the rendered (unfactored) form. Suite green. **Follow-up 2 (user):** the entropy cell now assigns `s = FullSimplify[...]` and the plot consumes it, `Plot[s /. {J -> 1, \[FormalT] -> t}, ...]`, eliminating the hand-copied formula that the old plot cell duplicated (the doc's last hardcoded-result wart); the user's literal `Plot[s, {t,...}]` needed the variable bindings since `s` carries `\[FormalT]`/symbolic `J` (verified `Plot` handles the `Quantity` itself fine). The explicit $S(t)$ formula dropped from the outcome sentence per instruction, replaced by the concise physical reading (binary entropy of the oscillating populations; zero at products, one bit at Bell points, periodic). Suite green (74 cells). |
| A60 | 1 | The inverse-design example (raw `MatrixExp` target + plain-WL $i\dot U U^\dagger$) was an acceptable QF showcase. | No (user: hardcoded, barely uses QF) | **Rebuilt QF-native.** The target trajectory is now composed from **named parametric gates**, `U = QuantumOperator["RZ"[\[FormalT]^2]] @ QuantumOperator["RX"[\[FormalT]]]` (no hand-written matrices, so the "design" is no longer circular), the dagger comes from the object (`U["Dagger"]`), and the recovered $H$ is wrapped as a `QuantumOperator` whose **`PauliDecompose` reads off the control fields by axis**: verified `<\|X -> Cos[t^2]/2, Y -> Sin[t^2]/2, Z -> t, I -> 0\|>` (clean trig via `FullSimplify@*ComplexExpand /@`; the zero identity coefficient = traceless control, named in the outcome sentence). Schrodinger identity $i\dot U - HU = 0$ re-verified exactly. Section is 2 cells (was 2). **Follow-up (user):** the last plain-WL step removed too: `H = I D[U, \[FormalT]] @ U["Dagger"]` works at the **operator level** (a `D` upvalue differentiates the parametric `QuantumOperator` directly, and `I op` / `op1 @ op2` stay in the algebra); identical verified decomposition `<\|X -> Cos[t^2]/2, Y -> Sin[t^2]/2, Z -> t, I -> 0\|>`. The section is now end-to-end QF: named gates -> operator calculus -> Pauli readout. Suite green (74 cells). |
| A59 | 4 | The TN `Hypergraph` view is not doc-safe (A55 recorded it erroring). | Corrected (user: add `qcv["TensorNetwork"]["Hypergraph"]` after `TensorNetworkGraph` and verify) | **A55's "Hypergraph errored" was a false alarm**: the property works and returns a `WolframInstitute`Hypergraph`Hypergraph`; my probe's `Quiet@Check` had tripped on two benign `ResourceObject::updavb` notices from stale cached resource functions (`MultiReplace`, `SubexpressionPositions`). `ResourceUpdate` on both -> the cell now runs **message-free**. **Added the cell to Claim 4's circuit section** as the dual picture (indices = vertices, each tensor = one hyperedge over its indices; contraction = hyperedge merging), with the verified structure in the outcome sentence: **13 hyperedges** = 3 initial $\|0\rangle$ states + 10 gates of GHZ+QFT. Full **70-cell** suite re-run green in document order. **Follow-up (user):** the Setup block moved from the opening to the **end** of the doc, a combined `Needs["Wolfram`QuantumFramework`"]` + `Needs["Wolfram`TensorNetworks`"];` block under "**Setup.** Evaluate once before the cells:" (the rendering pipeline can mark it as an initialization cell). **Follow-up 2 (user: those two loads must appear ONLY in the end block):** the inline `Needs["Wolfram`TensorNetworks`"];` removed from the ContractionTree cell; the suite driver now mirrors the setup block (loads both up front) and the full 70-cell run is green, the Tree cell resolving from the setup-supplied context. **Follow-up 4 (user):** the `SecondQuantization` sub-package `Needs` also moved from the optics cell into the end Setup block (now three loads there, the only `Needs` mentions in the doc); optics cell starts directly at `g2s = <|...|>`; suite driver mirrors all three, 70 cells green. **Follow-up 5 (user):** the basis-change cell upgraded from amplitude readout to formatted display: `QuantumState[QuantumState["+"], "X"] // TraditionalForm` (the `"X"` string is a basis shorthand, verified equivalent to `QuantumBasis["PauliX"]`); it renders as the single ket $\|x_+\rangle$, a sharper statement than $\{1,0\}$, and the outcome sentence was updated accordingly. Suite re-run green. **Follow-up 6 (user: replace the qubit $\langle X\rangle$ invariance pair with a higher-dimensional, interesting case):** now **spin-1, three bases at once**: $m_x{=}{+}1$ defined in the `"JX"[1]` basis (`{0,0,1}`), re-expressed via `"Computational"[3]` (amps $\{\tfrac12, \tfrac1{\sqrt2}, \tfrac12\}$ verified), expectation of the named $J_x$, which is itself stored in its own eigenbasis -> `<\|JX basis -> 1, computational basis -> 1\|>`. **Gotcha caught live:** bare `"Computational"` (no dimension) defaults to $d{=}2$ and silently truncates a qutrit state to $\{0,0\}$; the explicit `"Computational"[3]` is required. Flows into the adjacent spin-1 ZFS section. **Follow-up (user, after evaluating it themselves):** split into **three cells**, each with its own bridge: (1) `mx` defined + `// TraditionalForm` (renders $\|J_1^x\rangle$), (2) `mxZ` re-expressed + `// TraditionalForm` (renders $\tfrac12\|0\rangle + \tfrac1{\sqrt2}\|1\rangle + \tfrac12\|2\rangle$), (3) the two-representation expectation association; outputs not pasted, stated in the outcome sentences per house style. Suite green (72 cells). **Follow-up 7 (user: replace the numeric ZFS spin-1 example with their symbolic version):** $H = 2d\,J_z^2 + e\,(J_x^2 - J_y^2)$ with **formal symbols** `\[FormalD]`/`\[FormalE]` and operator **powers** (`Jz^2`, not `Jz[Jz]`); the eigenbasis now comes from the object's own property, `QuantumBasis[H["Eigenvectors"]]`, and the spectrum lands fully symbolic: $\operatorname{diag}(0, 2d-e, 2d+e)$. **Verified subtlety for the prose:** with power-arithmetic the operator's carried basis is the **$J_x$ eigenbasis** (basis names $J^x_m$, checked), so `H["Matrix"]` displays the $d+e/2$-cornered $J_x$-frame matrix while `H["Computational"]` is the textbook $\{\{2d,0,e\},\{0,0,0\},\{e,0,2d\}\}$, same spectrum; outcome sentence names the carried basis honestly. Also the qubit invariance pair's association echo dropped from its outcome sentence (user). Suite green (72 cells). **Follow-up 8 (user: add `PauliStabilizer["GHZ"]["TableauForm"]` to the visualization claim):** new Claim 4 section "A stabilizer state shows its tableau" between the measurement histogram and the circuit pair; verified output is a formatted `Row`/`Grid` (sign column + binary grid, dividers between $x$/$z$ blocks and destabilizer/stabilizer rows; GHZ generators readable in the bottom three rows). Narrative ties it to Claim 2's stabilizer section and lands the punchline "this view *is* the algebra" (Clifford gates rewrite columns of exactly this grid). Intro catalog gains the tableau entry. Suite green (**73 cells**). **Follow-up 3 (user: drop the opening verification paragraph, replace with a transition):** replaced by "The claims build on one another, symbolic objects at the base and real hardware at the summit, so we begin with the algebra everything else inherits." The verification facts (versions, captured outputs, `ibm_fez` run, qiskit/IBM-account requirements) compressed to one line under the end **Setup** block, so the title's "Verified" stays substantiated without front-matter. |
| A58 | all | The doc could keep internal cross-links, signature-verified hardware cells, and the long-sentence closing. | No (user: standalone external doc, no internal refs; verify everything incl. IBM with real credentials; concise consistent pedagogy; rewrite the closing carefully) | **(1) Externalized:** removed the pipeline-doc pointer and the open-systems companion parenthetical; zero internal references remain. **(2) IBM verified for real:** `ServiceConnect["IBMQuantumPlatform"]` with the user's API key (used transiently; credential-bearing scripts deleted; nothing recorded in any doc); backends = {ibm_fez, ibm_marrakesh, ibm_kingston} (no brisbane) -> doc cell repointed to **`ibm_fez`**; job `d8lpekb2d42s73cbnrm0`, 4096 shots, Queued -> Completed; `qpu = job["Refresh"][]` returned a `QuantumMeasurement`; the paired chart rendered (`Legended`). **Real device numbers now in the doc:** $0.428$ at $000$, $0.420$ at $111$, $\approx 15\%$ over the six forbidden bitstrings. Opening verification sentence upgraded: *nothing* in the doc is unexecuted anymore. **(3) Precision fixes from a full read:** stale Claim 4 cross-ref ("same property the job handle exposes" -> "same kind of object"); tomography ending no longer ropes the *continuous* Wigner function into the discrete dual-frame mechanism; "plotted by Mathematica" -> "drawn by the language's own `Plot`"; stray blank line. **(4) Closing rewritten** as three tight paragraphs: parallel per-claim recap (now incl. the real QPU histogram), the one-mechanism paragraph, a three-line reader handoff ending "The document re-derives itself." **(5) Final suite:** all 69 cells green in document order (QPU cells not resubmitted; this session's live results stand); signatures confirmed (Tsirelson $0.8536$, fidelity $0.99998$, entropy $496$, bound `True`); 0 dashes, 0 internal refs. |
| A57 | 2 | (user: add one good nontrivial open-systems example from `QF-OpenSystems-Showcase.md`) | Applied | Of the companion doc's six examples, picked **Example 1** (qubit relaxation solved once for *all* initial states and *all* rates): the most compact (3 cells), the most recognizable, and the purest statement of the showcase thesis (the answer is the general law, not a trajectory). **New Claim 2 section "The master equation, solved once for every state and every rate"** right after the channel section: symbolic $\rho_0 = $ `Array[\[Rho], {2,2}]` + symbolic rates -> `QuantumEvolve` -> the complete relaxation law (populations at $\gamma_1$, coherences at $\Gamma_2 = \gamma_1/2 + 2\gamma_\phi$ with $\pm i\Omega$ precession) -> `Reduce[ForAll[...]]` -> **$T_2 \le 2T_1$ as a theorem** (`True`), saturation at $\gamma_\phi = 0$ verified. Kept the companion's explicit $\sigma_-$ matrix convention (the named `"J-"` lowers spin the other way, the convention trap). **Drift check:** HEAD moved to `1c524432`, which includes the `10b99e7e` open-system `QuantumEvolve` fixes this example depends on; all 3 cells verified there. **Side-effect check:** the section sets a global `$Assumptions`; the full 69-cell re-run confirmed zero downstream simplification drift (Werner criterion, ring dispersion, stabilizer entropy 496, tomography fidelity all byte-identical). Cross-link to the six-example companion added at section end. Left in the companion (rejected for the main doc): entanglement sudden death (6 cells), Kossakowski/superradiance (longer), ME=channel=circuit (longer), `WhenEvent` control (its `"ReturnSolution"` caveat), Wigner flow (overlaps Claim 2's phase-space content). |
| A56 | 2 | (user: stabilizer formalism + methods must be included, PhD level; pointed to `QF-Stabilizer-Optimization-Report.md`) | Applied after a license saga | **New Claim 2 section "A stabilizer state: the state as its symmetry group"** (4 cells, all verified): `PauliStabilizer["GHZ"]["PauliForm"]` -> $\{XXX, ZZI, IZZ\}$ (Gottesman-Knill: state = generator tableau); deterministic expectations `<\|XXX -> 1, ZZI -> 1, ZII -> 0\|>` ($\mathbb F_2$ phase tracking, no state vector); **$n{=}1000$, 19,998 random Clifford gates via `ps["ApplyCircuit", gates]`** (the round-2 compiled fold from the optimization report) in **$\approx 0.5$ s**; half-chain entropy via the **Fattal closed form** -> exactly **496** ebits of 500 (Page-like). Prose carries the formalism (tableau, Gottesman-Knill, non-Clifford boundary -> `StabilizerFrame`). **Verification war story:** every probe containing `PauliStabilizer["Random", 500]` killed the kernel with a *spurious "license error"* message (6+ failed launches blamed on seat contention); bisection proved the ladder itself runs fine in 0.4 s and the Random-500 line was the killer -> dropped from the doc plan (the random *circuit* is the example anyway) and worth a kernel bug report. Per the report's caveat, gates are fed as **raw spec rules** (`"H" -> q`, `"CNOT" -> {c,t}`), not a `QuantumCircuitOperator` (whose 10^4-gate construction is the slow part). Threaded into Claim 2 intro (categories list), roadmap item 2, closing recap. **Full 66-cell suite re-run green, top to bottom, in document order.** |
| A55 | new claim | Visualization stays diluted (post-A38/A47 there was no views claim). | No (user: audit ALL QF visualizations; "rich visualization" is another unique feature; include BlochPlot, ProbabilityPlot, Diagram, AmplitudeChart, TN Hypergraph) | **Audited the full viz surface** (kernel grep + per-object property files): states `BlochPlot/BlochSpherePlot`, `AmplitudePlot/AmplitudeChart`, `AmplitudesPlot/AmplitudesChart`, `ProbabilityPlot/ProbabilityChart(s)`, `PieChart`, `SectorChart` (`QuantumState/Properties.m:36,993-1005`); measurements `Probability/ProbabilitiesPlot` (`QuantumMeasurement.m:210-212`); circuits `Diagram` (CircuitDraw), `TensorNetworkGraph`, multiway/causal/states/transition graph views; adiabatic `EnergySpectrumPlot/SpectralGapPlot/AdiabaticCouplingPlot/AdiabaticPathPlot`; TN `Hypergraph` + `ContractionTree`; Wigner surfaces. **New Claim 4 "Every object draws itself"** (5 cells, all verified by head: `Graphics3D`/`Legended`/`Graphics`/`Graphics`/`Graph`): Bloch sphere; **AmplitudesChart of QFT(GHZ)** (resurrects the A36 cell, now in a claim whose thesis is the user's: viz is property dispatch, not a library); measurement histogram (cross-ref to the QPU handle's same property); circuit `Diagram` + `TensorNetworkGraph` pair ("diagram shows the physics, network shows the computation"). Full audited catalog in the intro prose. **`Hypergraph` errored here** (fetches a WFR resource; `ResourceObject::updavb` fired) -> catalog mention attributed to the TensorNetworks paclet, no cell. Distinct from dropped old-Claim-5 "many views" (A38): that was a gallery without a message; this has one (one-call, object-owned, option-forwarding properties). **Interop renumbered Claim 4 -> 5** (hardware finale kept); opening roadmap now 5 items; closing recap updated ("Five claims", viz clause added). |
| A54 | 5 (interop) | The TN-path section's two cells (Greedy apply + Greedy===Optimal check) were the right depth. | Partially (user: drop the === cell; adopt contraction-tree + bond-dimension ideas from `TensorNetworks/Notebooks/Tests and explorations/TN-vs-BuiltinWL-report.nb`) | Read the TN report notebook (nb-reader): its pedagogy is the **contraction tree** (`ContractionTree[TensorNetworkContraction[net, path]]`), the **peak-intermediate / bond-dimension** cost story, and a measured **374x naive-vs-optimal-path** gap on a 4x5 chi=5 PEPS overlap. Applied: (1) dropped the `===` cell; (2) **new visualization cell**: `net = ghz["TensorNetwork"]` (public property, `QuantumCircuitOperator/Properties.m:414`) -> `ContractionTree[TensorNetworkContraction[net, GreedyContractionPath[net]], "Labels" -> "Dimensions"]` (verified: `Tree`, message-free; needs `Needs["Wolfram`TensorNetworks`"]`); (3) intro prose now teaches cost via the largest-intermediate / bond-dimension language instead of bare "treewidth". Section = apply-with-path cell + tree cell. |
| A53 | closing | A bare "Reproducibility" paragraph was the right way to end the document. | No (user: drop it; replace with a "where this leaves us" / concluding-remarks wrap, written very carefully) | **Replaced the final section with "Where This Leaves Us"**, three paragraphs: (1) the four claims recapped through their *computed* results (Rabi propagator, parabolic-cylinder LZ, exact QAOA landscape, $S(t)$; purity loss $2\gamma(1-\gamma)\sin^4\tfrac\theta2$, 4-outcome POVM, $g^{(2)}$, negative Wigner, basis+dimension as object properties, tomography $\approx 0.99998$; WL consumption; the measured circuit's QASM->transpile->QPU arc), per the learning-by-computing closing convention (recap what was *computed*, not asserted); (2) the one-sentence takeaway (the mechanism: exact symbolic objects composing with each other and the language) + reader-facing next steps (change the graph, raise the spin, swap the channel, repoint the backend: "the document re-derives itself"); (3) the reproducibility facts retained verbatim-in-substance as a closing "what executed means here" note (single-session run, qiskit-bridge cells, three token-gated hardware cells). No cells changed; doc stays 55 cells, 0 dashes. **Follow-up (user: function names silly in a closing):** the Claim 3 recap clause de-listed, "`Plot`, `Reduce`, and `Association`s consumed them" -> "the rest of the language worked on them directly, with nothing converted in between"; a conclusion speaks at the level of ideas, the sections own the function names. **Follow-up 2 (user: the "what executed means" paragraph is a weak ending):** deleted the closing caveat paragraph; its honesty load moved compactly into the *opening's* verification sentence ("the only exceptions are the three hardware cells, which need an IBM account and are verified against the kernel source instead; the two transpilation cells run through the Python bridge and need qiskit" + "figures regenerate from their cells"). The document now ends on the strong line: "...and the document re-derives itself." |
| A52 | 4 | Submitting the bare `QuantumCircuitOperator["GHZ"[3]]` to `IBMJobSubmit` was a valid example. | No (user: won't work, no measurement; adopt the `IBMJobSubmit.md` doc-page comparison) | A sampler job needs a circuit that **ends in a measurement**; the doc page's house idiom is a qubit-index list as a circuit element (`{"GHZ", {1, 2, 3}}`). **Rewrote the IBM section on the doc-page pattern** (`Documentation/.../IBMJobSubmit.md`): (1) `qc = QuantumCircuitOperator[{"GHZ"[3], {1, 2, 3}}]` + submit + status; (2) `wl = qc[]` runs the same circuit exactly in WL (verified: returns a `QuantumMeasurement` with the GHZ $\tfrac12/\tfrac12$ distribution); (3) `qpu = job["Refresh"][]` fetches the hardware `QuantumMeasurement`; (4) the doc-page **paired comparison chart** `BarChart[Transpose[Values @* KeySort /@ {wl["Probabilities"], qpu["Probabilities"]}], ..., ChartLegends -> {"Exact WL", "QPU"}, PlotLabels -> Keys[...]]` replaces the two separate `ProbabilitiesPlot` cells (chart shape verified against a stand-in measurement; `Legended`). Section retitled "...compare to the exact result"; heading's point sharpened: same object kind on both sides $\Rightarrow$ device noise read off one figure. Reproducibility note updated (three token-gated cells: submit, fetch, chart). Doc 54 -> **55 cells**, full suite re-run green. |
| A51 | 2, 3 | Only the Bell property-dispatch cell needed the Association treatment (A50). | No (user: do it everywhere similar) | **Association sweep over every bare heterogeneous/comparison list.** Converted four more cells to self-labeling `Association`s, each verified message-free and the full 54-cell suite re-run green: **Werner** (Claim 3): `Simplify /@ <\|"Purity" -> ..., "Entropy" -> ...\|>` (note: `Simplify` does not thread over an `Association`, so it is mapped with `/@`); **SIC-POVM** (Claim 2): `<\|Outcomes -> 4, QuditDimension -> 2, POVMQ -> True\|>` replaces `{4, 2, True}`; **$g^{(2)}$** (Claim 2): `g2s` is now keyed `Fock/Coherent/Thermal` (verified `BarChart` consumes the association unchanged); **$\langle X\rangle$ pair** (Claim 2 basis section): keyed `computational basis`/`X basis`, making the two-representations point self-documenting. Left as lists: homogeneous same-quantity vectors where position IS the meaning (amplitudes, probability lists, the ring dispersion, tomography `Values[data]` whose natural keys are unwieldy measurement-operator display forms) and built-in outputs (`FindMaximumCut`, `NMaximize`). Doc-wide count: six Association outcomes (these four + Bell dispatch + `PauliDecompose`). Outcome sentences rewritten per A46. |
| A50 | 3 | The property-dispatch cell's bare-list outcome `{1, Quantity[0,"Bits"], True, 1}` was readable. | No (user: show it better, as an Association/TableForm) | Recast the cell to return a **self-labeling `Association`** (`<\|"Purity" -> bell["Purity"], "Entropy" -> ..., "EntangledQ" -> ..., "Concurrence" -> ...\|>`), so the output carries its own keys instead of forcing the reader to map list positions to meanings (verified message-free). Side benefit: Claim 3's first section now returns an `Association` just before the `PauliDecompose` section does, reinforcing the dispatch-into-WL-data theme. Outcome sentence rewritten per the A46 rule, with the association shown as a code span (it is code, not math). Doc still 54 cells. |
| A49 | 2 | `1 - QuantumDistance[..., "Fidelity"]` was the right tomography-score idiom, and the bridge's $F = (\operatorname{Tr}\sqrt{\sqrt\rho\,\sigma\sqrt\rho})^2$ convention matched it. | No on both (user asked: replace with `QuantumSimilarity`? audit) | **Audited the kernel** (`QuantumDistance.m`): `QuantumSimilarity[qs1, qs2]` defaults to `"Fidelity"` and is *literally* `1 - QuantumDistance[qs1, qs2, "Fidelity"]` (`:63-67`), so it is a clean drop-in; cell replaced with `QuantumSimilarity[true, est["MaximumLikelihoodState"]]` (verified identical, $0.9999845\dots$, message-free). **Bonus convention bug caught:** the kernel computes $1-\operatorname{Re}\operatorname{Tr}\sqrt{\rho_1\rho_2}$ (`:15-16`), and $\operatorname{Tr}\sqrt{\rho_1\rho_2}=\operatorname{Tr}\sqrt{\sqrt{\rho_1}\rho_2\sqrt{\rho_1}}$ (similar matrices), so the similarity is the **square-root (Nielsen-Chuang) fidelity**, not the squared (Jozsa) one the bridge displayed; verified directly on the pure true state ($\sqrt{\langle\psi\|\sigma\|\psi\rangle}=0.9999845$ matches). Bridge formula corrected to $F=\operatorname{Tr}\sqrt{\sqrt\rho\,\sigma\sqrt\rho}$ and the "1 minus QF's fidelity distance" parenthetical dropped (no longer needed). The audit's standing note ("`QuantumDistance` Fidelity returns $1-F$, use `QuantumSimilarity` for $F$") was the pointer. |
| A48 | 3, opening | "First-class Wolfram Language expression" is a safe description of a QF object. | No (user: Wolfram R&D would not accept it) | In WL-internal vocabulary, "first-class" connotes kernel-level System` citizens with unified-design integration (`Graph`, `Image`, `Dataset` style compound atomics); a paclet-defined wrapped head is not that, so the phrase overclaims status to exactly the audience that can tell. **Replaced uniformly with the lower, truer, and rhetorically stronger framing: a quantum object is an *ordinary* WL expression**, and that ordinariness is *why* the language composes with it. Edits: Claim 3 heading -> "...an ordinary Wolfram Language expression"; opening roadmap item 3 gains "precisely because nothing about it is special"; Claim 3 key-feature line varied to "a symbolic expression like any other in the language, with no separate runtime or data format behind it... nothing to convert or export first" (avoids echoing the heading); Claim 2's "represents as first-class objects" -> "as objects you compute with directly". Zero "first-class" remains in the showcase. Map row label updated. Prose-only; no cells touched. |
| A47 | all | The basis claim deserved to stand as Claim 5, and the claim order (interop before basis) was fine. | No (user: order bad, esp. Claim 5; not confident it should exist; asked for a cross-package check + keep/dilute suggestion) | **Cross-comparison run live** (reinstalled qiskit 2.4.1 / qutip 5.3.0 / pennylane 0.45.0 in `/tmp/qcbench` after a /tmp wipe): Pauli decomposition is a **commodity** (`SparsePauliOp.from_operator` and `qml.pauli_decompose` both reproduce the identical six H$_2$ coefficients); QuTiP has `Qobj.transform(U)` (manual change of basis) and `jmat` spin ops; tomography is ecosystem-standard (qiskit-experiments). **Unique to QF** (verified earlier from source): basis stored as an attribute on every object with automatic reconciliation (no basis attribute exists on `Statevector` or `Qobj`, verified), the 37-name basis catalog + dual-frame engine, uniform any-finite-dimension. Verdict: the claim's *mechanism* is unique but is **infrastructure** (it "underwrites" QASM rebasing/KAK per the comparison doc), while its visible examples under-differentiate, and ending the doc on basis bookkeeping after the QPU climax was anticlimactic. Offered dilute / keep+sharpen / reorder-only; user chose **dilute**. **Restructure (5 -> 4 claims):** Claim 2 gained three sections ("Every object carries its basis" = condensed mechanism + $\|+\rangle$ rep-vs-physics pair; "Any finite dimension" = spin-1 ZFS + $N{=}5$ ring; "State estimation" = the 3-cell tomography loop) and its intro now names estimation + the two "quiet generalities" (basis, dimension). Claim 3 gained "`PauliDecompose` -> `Association`" (reframed as dispatch returning WL data, where it earns its place by integration, not uniqueness). **Dropped**: Weyl-Heisenberg/Dirac section (esoteric; catalog gets one line in the mechanism para) and the standalone custom-eigenbasis pitch (the spin-1 section's eigenbasis cell carries it). Opening: "five claims" -> "four", roadmap item 5 removed, item 2 expanded. Doc 56 -> **54 cells**, re-extracted and **re-run top-to-bottom in the new order, all green** (relocated cells byte-identical; no variable collisions: spin-1's `H` overwrite lands after Claim 1's last `H` use, ring's `n=5` after QAOA's `n=3`). Hardware now closes the document. |
| A46 | all | Displaying a computed closed form as a standalone `$$label = formula$$` block is a fine way to present a cell's outcome. | No (user: discuss outcomes as prose with inline math, like the Claim 1 amplitudes line; the Bloch-vector display block is the anti-pattern) | **Outcome-prose pass.** Converted all nine outcome-statement display blocks into interpretation sentences with inline TeX ("The X returns exactly as $\dots$: gloss"): Bloch vector, Rabi formula (also trimmed the bridge that pre-announced "the textbook Rabi formula" to avoid saying it twice), inverse-design $H(t)$, QAOA landscape, Heisenberg $\|\psi(t)\rangle$ (gained a physical gloss: excitation oscillates coherently), $S(t)$, purity loss, Werner purity+entropy pair, realignment criterion + `Reduce` threshold (merged into one sentence covering both cells). **Kept exactly one display block**: the $2\times2$ Rabi propagator matrix, which cannot be inlined readably. **Follow-up (user: still read as a dropped output):** the display alone after the cell was not enough; restructured so the *outcome sentence comes between cell and display* ("The matrix returns exactly as the lab-frame Rabi propagator, written here with $\delta \equiv \omega-\omega_0$ and $\Omega_R \equiv \sqrt{\Omega^2+\delta^2}$:"), with the bridge before the cell reduced to the action ("Simplify its matrix under real parameters") and the gloss after unchanged. Rule sharpened: even a kept display must be *introduced by an outcome sentence*, not merely bridged and glossed. Prose-only edits; no cells changed; 56 cells, 0 dashes. **Rule for future examples: a computed outcome is discussed in a sentence with the closed form inline; display math is reserved for genuinely two-dimensional results (matrices).** |
| A45 | opening | The meta-style opening ("Five differentiators... cut along that claim's natural axis" + a buried one-sentence positioning) introduced the doc adequately. | No (user: rewrite fully; position QF properly per the Platform Comparison doc; about QF, not other packages; highlight what the doc elaborates) | **Rewrote the opening completely.** Re-read `QF-Master-Platform-Comparison.md` in full first; took its positioning core (symbolic-first object algebra; the genuinely-unique list in its §5: exact symbolic objects, one model across all of quantum theory, basis as a first-class attribute, native any-dimension, WL-integration moat; "decision guide" framing: deliverable = derivation/insight) and recast it **entirely as statements about QF**, zero comparison framing (consistent with the doc-wide positive-tone rule). New shape: (1) identity paragraph: a quantum laboratory inside WL, objects are exact symbolic expressions that carry parameters / compose in closed form / answer questions through one interface; computations end in formulas ($U(t)$, landscapes, $S(t)$); any finite dimension; reaches outward to QASM/transpile/hardware; "natural habitat: the computation whose deliverable is insight." (2) A 5-item roadmap using the **exact claim titles**, one line of foreshadowed content each (replaces the internal "natural axis" design language, which stays in this pipeline doc). (3) The verification sentence upgraded to "top to bottom in one session" (true since A44). Setup cell and pipeline pointer kept; the old "One-sentence positioning" line absorbed into paragraph 1 and deleted. **Follow-up (user: "tweak it down"):** roadmap item 3's "flows directly into `Plot`, `Reduce`, and the rest of the language, with no glue code" toned to "Built-ins like `Plot` and `Reduce` apply to it directly", dropping the sweep ("rest of the language") and the slogan ("no glue code", slightly overstated anyway given the Claim 3 `NumericQ` wrapper). **Follow-up 2 (user):** roadmap item 4 de-vendored: "submission to IBM quantum hardware" -> "submission to a real QPU, with IBM as the worked example" (OpenQASM is the vendor-neutral door; the hub's provider path also carries an AWS-Braket conversion; the Claim 4 section itself rightly stays IBM as the example). |
| A44 | all | The doc was internally consistent and every cell's displayed output matched what the cell returns. | Partially (full-document audit found real drift) | **Whole-doc revision pass**: extracted all 56 cells and ran them **top-to-bottom in a single kernel** (as a reader would), then read the prose end-to-end. All cells green, but four output-honesty gaps and one real API bug surfaced. **(1) Transpile cell was broken twice over**: `QuantumQASM[qco, target]` with a *positional* `QiskitTarget` matches **no rule** (returns unevaluated `QuantumQASM[QiskitCircuit[...], QiskitTarget[...]]`; the rule at `QuantumQASM.m:311` takes `basisGates:{___String}\|Automatic` then options) -> correct form is the **`"Target" -> target` option**; and qiskit's `Target.from_configuration` **requires `basis_gates`** -> spec must carry `"BasisGates"`. With both fixes the transpile **runs and verifies live** (GHZ -> `rz`/`sx`/`cx` on `$0,$1,$2` respecting the chain coupling): qiskit is reachable via the WL Python bridge after all, so the Reproducibility note was wrong to exclude it; now only `IBMJobSubmit` + device chart are unexecuted. Also corrected: `QiskitTarget` construction is **not** native WL, it schema-validates via `PythonEvaluate` (`QuantumQASM.m:160`). **(2) Output-honesty fixes** (cell returned an equivalent-but-uglier form than the doc displayed): `BlochVector` -> add `ComplexExpand//FullSimplify` (raw form is `Conjugate`-laden); Heisenberg entropy -> `FullSimplify[..., \[FormalT] J \[Element] Reals]`; Werner realignment -> `Simplify[..., 0 < p < 1]` (now returns the displayed $(\|3-4p\|-1)/2$). **(3) Repetition/wording**: stale "(Claims 1, 3, 5)" cross-ref from the pre-A38 numbering -> "(Claims 1 and 3)"; duplicated "None of this is special to qubits" (intro + spin-1 opener) -> deduped; "Maximize it analytically" vs the `NMaximize` cell -> "Maximize it over the two angles" + "an exact objective optimized directly, with no sampling noise"; "warm-ups" twice -> once; `BarChart[{0,1,2}]` hardcode -> chart the computed `g2s`; "first-class" twice in 3 lines of Claim 2 -> once; "QF confirms" on a plain-matrix check -> "This $H$ checks out". All edited cells re-verified in their exact new form. |
| A43 | all | A prose bridge naming *what* a quantity is (e.g. "$g^{(2)}(0)$ classifies light by its photon statistics") is enough. | No (user: each example's bridge must carry the closed-form formula of the quantity computed next) | **Formula-bridge pass.** Added a concise closed-form definition to every example whose computed quantity lacked one, keeping it to one formula + a short gloss (user: "pedagogical but not too much"). Added: **Claim 2** $g^{(2)}(0)=\langle a^\dagger a^\dagger aa\rangle/\langle a^\dagger a\rangle^2=\langle n(n-1)\rangle/\langle n\rangle^2$ (the flagged exemplar); POVM $\sum_i E_i=I$, $p_i=\operatorname{Tr}[\rho E_i]$, SIC overlap $|\langle\psi_i|\psi_j\rangle|^2=1/(d+1)$; Wigner $W(x,p)=\tfrac1\pi\!\int\langle x{-}y|\rho|x{+}y\rangle e^{2ipy}dy$; purity $\operatorname{Tr}\rho^2$. **Claim 3** purity / $S=-\operatorname{Tr}[\rho\log_2\rho]$ / concurrence in the dispatch bridge. **Claim 5** $\langle X\rangle=\langle\psi|X|\psi\rangle$; Schwinger expansion $A=\sum_{ab}c_{ab}X^aZ^b$; Pauli $H=\sum_\alpha h_\alpha P_\alpha$, $h_\alpha=\operatorname{Tr}[P_\alpha H]/2^n$; fidelity $F=(\operatorname{Tr}\sqrt{\sqrt\rho\,\sigma\sqrt\rho})^2$. **Left as-is:** Claim 1 (already formula-dense: Rabi, Landau-Zener $e^{-\pi\Delta^2/2v}$, $H=i\dot UU^\dagger$, QAOA landscape, $S(t)$) and Claim 4's pure format/interop steps (export/transpile/IBM compute a *conversion*, not a physical quantity; the CHSH cell already carries $\Pr[x\wedge y=a\oplus b]=\cos^2\tfrac\pi8$). Prose-only edits; no cells changed; dashes 0, math delimiters balanced. |
| A42 | 5 | Claim 5's examples adequately showed QF's basis machinery. | No (user: "nowhere we mentioned QF supports any finite dimension") | Every example was effectively two-level / $2^n$. **Added two genuinely non-qubit examples** and made the dimension-agnostic claim explicit in the intro. (1) **Replaced the abstract $2\times2$ custom-eigenbasis with a spin-1 (qutrit) NV-center zero-field-splitting Hamiltonian** $H=D J_z^2+E(J_x^2-J_y^2)$ built from QF's **native spin-1 operators** `QuantumOperator["JX"/"JY"/"JZ"[1]]` (verified they exist; `JZ[1]`$=\operatorname{diag}(-1,0,1)$, each stored diagonal in its own eigenbasis and reconciled automatically), $H=\{\{2,0,1\},\{0,0,0\},\{1,0,2\}\}$ -> eigenbasis $\operatorname{diag}(3,1,0)$. (2) **New $N{=}5$-site ring section:** hopping Hamiltonian is diagonal in `QuantumBasis["Fourier"[5]]` with the tight-binding dispersion $\{-2,-0.618,1.618,1.618,-0.618\}=-2\cos(2\pi k/5)$ (a dimension-5, non-power-of-2 system). **Gotcha:** the Fourier-basis matrix is exact-symbolic ($e^{2\pi i/5}$); `Chop` alone leaves the off-diagonal cancellations unsimplified, so the cell needs `Normal@Chop@N@Diagonal[...]` to display the clean list. Intro now states states/operators live in any finite dimension (qutrit, spin-$j$, $N$-level). Claim 5 now 6 sections / 11 cells; doc 54 -> 56 cells. **Then dropped the "What we computed" recap section** (user) -> Claim 5 ends on the tomography result; the recapped facts (incl. "~three dozen bases" + QASM/KAK) already live in the intro, nothing lost. |
| A41 | 5 | The MUB section (measure $\|0\rangle$ in $Z/X/Y$ -> $\{1,0\}$/$\{\tfrac12,\tfrac12\}$) was a good tomography illustration. | No (user: use `QuantumStateEstimate` etc., generate synthetic data) | **Replaced the static MUB-probabilities cell with a real tomography loop.** Read the kernel (`QuantumStateEstimation.m`, 1072 LOC, Mihai Vidrighin): `QuantumMeasurementSimulation[state, {qmos}, n]` multinomially samples synthetic counts per measurement (`AssociationMap[qmo -> RandomVariate[MultinomialDistribution[n, qmo[state]["ProbabilitiesList"]]]]`); `QuantumStateEstimate[data]` runs linear inversion -> physical projection -> maximum-likelihood (300 iters) -> Bayesian, returning `QuantumStateEstimation[<|14 keys|>]` (`InversionState`, `MaximumLikelihoodState`, `BayesianSampler`, ...). **New 3-cell section:** `SeedRandom[42]`; unknown $\cos\tfrac\pi6\|0\rangle+\sin\tfrac\pi6 e^{i\pi/4}\|1\rangle$; simulate 2000 shots in $X,Y,Z$ -> counts $\{\{1605,395\},\{1620,380\},\{1504,496\}\}$; `QuantumStateEstimate` -> reconstructed $\rho$; fidelity $\approx 0.99998$ to truth. **Gotchas:** (a) the true state **must be numeric** (`N@{...}`) or `QuantumDistance` hits `N::meprec` on the symbolic $\sqrt{\rho_1\rho_2}$ (no-`Quiet`); (b) `QuantumDistance[...,"Fidelity"]` returns $1-F$, so fidelity $= 1 -$ that; (c) the ML estimator uses randomness but is **reproducible** under `SeedRandom` (verified identical across two runs). The X/Y/Z MUB content is now embedded (they are the IC measurement set). Kept the dual-frame -> Claim 2 pointer. Claim 5 now 5 sections / **9 cells** (MUB was 1 cell -> tomography 3); doc 52 -> 54 cells. |
| A40 | 5 | The reconciliation section (fidelity $=0$; $Z\|+\rangle=\|-\rangle$) and the Bell-basis section were good examples. | No (user: "silly trivial"; "drop Bell") | **Replaced the reconciliation cells** with two exotic operator-basis reads, per user request for Dirac/Schwinger: qutrit cyclic shift -> a **single Weyl-Heisenberg (Schwinger) coordinate** ($\{0,0,0,0,1,0,0,0,0\}$), and $X\otimes Y$ -> a **single Dirac covariant** ($\{0,\dots,0,1\}$). **Key kernel finding:** the operator-decomposition idiom is `QuantumState[op["Double"], QuantumBasis["Schwinger"/"Dirac"]]["StateVector"]` (the `["Double"]` vectorization, same as the Wigner transform); the naive `QuantumState[Flatten[op], basis]` gives the *wrong* coordinates (Schwinger/Dirac operators are orthonormal under the trace inner product, not the vector one), and QF has **no** general "decompose in basis B" property beyond `PauliDecompose`. Did not assert specific element labels (the StateVector is in canonical-sorted, not Names, order). **Dropped the Bell-basis section** (user) -> entanglement-visible-via-measurement-basis is gone from Claim 5. Heading "Computing across two bases..." -> "The Weyl-Heisenberg and Dirac operator bases". Claim 5 now 5 sections / 7 cells; doc 54 -> 52 cells. |
| A39 | 5 | The 9-section basis claim (S1-S9) was the right length and coverage. | No (user: "too long; add the eigenbasis cell, drop repeats") | Audited a machinery-centered 20-item PhD basis menu against the live kernel; **most of it does not survive as showcase material**: the flagship "mixed-basis matrix element / independent operator input-output legs" throws an internal assertion (`Order === FullOrder`) on the 2-qubit braket, the "channel as Pauli transfer matrix" has no such `QuantumChannel` property, and KAK's magic basis is internal-only (no clean cell). The one verified-clean gain: a **custom basis from eigenvectors** diagonalizes an operator (`QuantumOperator[QuantumOperator[H], QuantumBasis[Eigenvectors[H]]]["Matrix"]` -> $\operatorname{diag}(5,0)$). **Rebuild (9 -> 6 sections, 16 -> 10 cells):** (1) folded the *mechanism* into the intro ($B$ is a stored matrix; change of basis $=B^{-1}AB$; reconcile via the computational frame; the same engine powers QASM measurement rebasing + KAK magic basis), answering the earlier "it's not clear how"; (2) **replaced** the Dicke "eigenbasis of a symmetry" section with the cleaner **custom-eigenbasis** cell (both were "eigenbasis simplifies"; the new one is a literal user-defined basis change); (3) **dropped Schmidt** (entanglement entropy overlaps the Claim 1 Heisenberg $S(t)$ capstone), **Wigner negativity and SIC/QBism** (both already worked in Claim 2 as object categories) -> kept a one-sentence dual-frame pointer to Claim 2 instead of two redundant cells. Kept: S1 invariant, S2 reconcile, S4 Pauli, S5 Bell, S7 MUB. New cell verified message-free; total doc 61 -> 55 cells. **Follow-up:** user then **dropped the $\ell_1$-coherence cell** from S1 (and its intro/summary mentions); S1 is now just "amplitudes move ($\{1,0\}$), invariant $\langle X\rangle=1$ stays" -> 54 cells. |
| A38 | 5, all | "Many faithful representations" (old Claim 5: Bloch / diagram+matrix / tensor network / amplitudes chart) is a strong standalone claim. | No (user: "I do not get the purpose / main message") | Diagnosed and **dropped old Claim 5 entirely**. The honest read: "many views" reads as a *gallery of render methods*, not a claim; the one consequential point (single source of truth $\Rightarrow$ guaranteed-consistent views) was asserted, never demonstrated; and it **overlaps** Claim 3 (first-class WL expression, "one object, many questions") and the basis claim ("one object, many representations"), the *one object, many X* template, with $X=$ renderings being the thinnest. Offered sharpen / merge-into-3 / drop / keep; user chose **drop**. Consequences reconciled: showcase intro "Six -> Five differentiators" (removed "view type (Claim 5)"); the Claim-4 "Boundary with Claim 5" parenthetical removed; **old Claim 6 (basis) renumbered to Claim 5**; example map row deleted + renumbered; boundary line trimmed to two clauses. Doc is now **five claims, ~49 cells**. Lesson: a differentiator must name a *consequence a competitor lacks*, not list capabilities the object happens to expose; "you can render it many ways" fails that test. |
| A37 | 6 | Claim 6's original ordering (Dicke + $\|{+}{+}{+}\rangle$ crammed together, reconcile $\times 2$, Pauli, MUB, Wigner, SIC) was coherent. | No (user: "all over the place and not consistent") | **Major restructure of Claim 6** to a single throughline ("QF carries the basis: re-expressing is one call, the invariants survive, and you pick the basis where a structure becomes simple") with a uniform learning-by-computing rhythm and **one idea per section**. Audited a PhD-level 50-item basis list against the live kernel first; folded in the three highest-yield picks. **New S1 (pick 1, the headline):** $\|+\rangle$ in $Z$ vs $X$ basis: amplitudes $\{1/\sqrt2,1/\sqrt2\}\to\{1,0\}$, invariant $\langle X\rangle=1$ from **both** reps (clean bra-ket `ps["Dagger"][QuantumOperator["X"][ps]]["Scalar"]`), $\ell_1$-coherence $1\to0$ (basis-relative). This subsumes and retires the old $\|{+}{+}{+}\rangle$ point. **New S5 (pick 4):** same Bell state reads $\{\tfrac12,0,0,\tfrac12\}$ (classical-looking) in the computational basis vs deterministic $\{1,0,0,0\}$ in the Bell basis via `QuantumCircuitOperator["Bell"]["Dagger"]`. **New S6 (pick 5):** Schmidt `["SchmidtBasis"]["Disentangle"]` $\to$ 2 terms; `QuantumPartialTrace[bell,{2}]["VonNeumannEntropy"]` $\to$ 1 bit. Kept/retitled: Dicke now "eigenbasis of a symmetry" (S3), reconcile tightened (S2), Pauli (S4), MUB/Wigner/SIC (S7-9). **Gotchas found & avoided:** (a) `Tr[psX["DensityMatrix"].PauliMatrix[1]]` $=0$ is the *wrong-basis failure mode itself* (mixes $X$-basis $\rho$ with $Z$-basis op) -> must use basis-carrying objects; (b) `QuantumMeasurementOperator["X"]["Mean"]` $=0$ uses outcome labels $\{0,1\}$, not Pauli eigenvalues $\pm1$; (c) the `Magic` transformer route to a Bell measurement fires a benign `QuantumCircuitOperator::dim` message -> used inverse Bell-prep instead (message-free). All 17 cells re-verified message-free in one pass. |

---

## 3. Claim 1 case study: critique and redesign

### 3.1 The questions raised (and the honest answers)

**Who is the target audience for the `QuantumEvolve` example?**
At best a student or educator who recognizes a Rabi oscillation in $\cos t\,\lvert0\rangle - i\sin t\,\lvert1\rangle$. It does **not** hook researchers or industry: the result is textbook, hand-derivable, and contains no parameter in the *model* (only time appears, and only because the solver introduces it). By gate 4 of the pipeline it serves at most two audiences, weakly. The critique is correct: it is the most trivial example in the set.

**Why `QuantumEvolve` and not a symbolic Hamiltonian (`QuantumOperator`) and symbolic initial state (`QuantumState`)?**
This was assumption A1. I reached for the function whose uniqueness is most *visible* (`DSolve` under the hood) and let it represent the claim. That narrows a pervasive property to one function. The deeper, truer demonstration is that QF objects are symbolic *everywhere*: you can build $H(J,h) = J\,Z + h\,X$ as a `QuantumOperator` with free parameters, prepare a symbolic `QuantumState`, and read off exact properties, all without evolving anything.

**Why not a parametric / symbolic `QuantumCircuitOperator`?**
This is the strongest missing piece, and the one that actually reaches researchers and industry. A parametric circuit (the shape of every VQE / QAOA ansatz) compiles in QF to a **closed-form unitary in the angles**, and its observable expectation is a **closed-form cost landscape**. In Qiskit you bind parameters and run a statevector simulation per point; in QF you get the function. That is a genuine contrast-test win, and the first draft omitted it entirely.

### 3.2 The redesign: a symbolic sequence (all examples kernel-verified)

Instead of one trivial evolution, build up from recognizable to consequential. Every output below was captured from a live kernel (QF 2.0.0, WL 15.0).

**Example 1, symbolic operator** (students/educators recognize the gate):
```wolfram
QuantumOperator["RX"[\[Theta]]]["Matrix"] // Normal
```
$$
\begin{pmatrix}\cos\frac{\theta}{2} & -i\sin\frac{\theta}{2}\\[1mm] -i\sin\frac{\theta}{2} & \cos\frac{\theta}{2}\end{pmatrix}
$$

**Example 2, symbolic Hamiltonian with free parameters** (the model itself is symbolic):
```wolfram
H = J QuantumOperator["Z"] + h QuantumOperator["X"];
H["Matrix"] // Normal
```
$$
\begin{pmatrix}J & h\\ h & -J\end{pmatrix}
$$

**Example 3, symbolic state property** (one symbolic state, exact geometry):
```wolfram
QuantumState[{Cos[\[Alpha]], Sin[\[Alpha]] Exp[I \[Beta]]}]["BlochVector"]
```
$$
\bigl(\sin 2\alpha\,\cos\beta,\ \ \sin 2\alpha\,\sin\beta,\ \ \cos 2\alpha\bigr)
$$

**Example 4, parametric circuit to closed-form cost** (the researcher/industry hook):
```wolfram
ansatz = QuantumCircuitOperator[{"RY"[\[Theta]] -> 1, "RZ"[\[Phi]] -> 1}];
psi = ansatz[QuantumState["0"]];
v = Normal[psi["StateVector"]];
FullSimplify[Conjugate[v] . Normal[QuantumOperator["Z"]["Matrix"]] . v,
  Assumptions -> {\[Theta] \[Element] Reals, \[Phi] \[Element] Reals}]
```
$$
\langle Z \rangle(\theta,\phi) = \cos\theta
$$
The full variational cost landscape, in closed form, with zero sampling. (Optionally, example 5 is `QuantumEvolve` for symbolic dynamics, now demoted to "and time evolution is symbolic too," not the headline.)

### 3.3 Why the sequence serves all four audiences

| Audience | What hooks them | Example |
|---|---|---|
| Student | Recognizes $R_X(\theta)$ and the Bloch parametrization as exact algebra, not floats | 1, 3 |
| Educator | Can derive each example on the board and reproduce it identically | 1, 2, 3 |
| Researcher | Symbolic Hamiltonian parameters and a closed-form VQE cost landscape | 2, 4 |
| Industry | Parametric ansatz to analytic cost: optimize the function, do not sample it | 4 |

### 3.4 Verification record
- Probes: `/tmp/qfshow/c1alt.wls`, `c1circ.wls`, `c1exp.wls`, `c1final.wls`.
- `QuantumState["ExpectationValue", ...]` and `QuantumOperator["Expectation", ...]` are **not** valid; expectation was computed as $\langle\psi|Z|\psi\rangle$ directly. (Open item: find the QF-native expectation idiom, if one exists, rather than the manual braket.)
- All symbolic simplifications used explicit real-variable assumptions; without them QF correctly keeps `Conjugate[...]` terms, which is a feature, not a bug.

---

## 4. Status / open decisions

- [x] Apply the Claim 1 redesign to [QF-Showcase.md](QF-Showcase.md). **Done**, restructured as one example per audience.
- [x] Re-audit Claims 2 to 5 and rebuild as one verified example per audience (20 total). **Done.** All but one line (Claim 4 industry, hardware submission) executed in-kernel.
- [x] Switch structure to one example per audience. **Done** (supersedes A8). *Note: this "per audience" phase (the three items above) was itself later superseded by per-claim natural-axis cuts (A10, A12); §5 reflects the current structure.*
- [x] Find a QF-native expectation-value idiom. **Found (user-supplied):** a `QuantumCircuitOperator` accepts **states as elements**. `QuantumCircuitOperator[{psi, op -> 1, psi["Dagger"]}]` compiles to a 0-qudit operator whose scalar value is $\langle\psi|op|\psi\rangle$ (ket first, bra `["Dagger"]` last). Verified: gives $a\cos\alpha+b\sin\alpha$ for the VQE Hamiltonian, and $\cos\frac\theta2\cos\frac\phi2 - i\cos(\alpha+\frac\theta2)\sin\frac\phi2$ for a unitary. Replaces the manual braket in Claim 1's last example. (`["ExpectationValue"]`/`["Expectation"]` are still not valid; this is the idiomatic route.)
- [x] Claim 4 rebuilt on the **current** interop API (`QuantumQASM`, `QiskitTarget`, `IBMJobSubmit`, `IBMJob`); native export+import now runnable/verified (A22).
- [x] Added **Claim 6** (basis handling), rewritten in learning-by-computing style with exotic/phase-space bases (A23, A24).
- [x] Removed numeric-comparison tone (A18); removed embedded plots, code-only (A19); "rungs"->"examples" (A20); moved to `OngoingProjects/Why QF/` (A21).
- [ ] Visuals: plot images are not embedded (only generating code). Re-embed when wanted; regenerate from `/tmp/qfshow/regen_final.wls`.
- [x] Claim 4 transpile **executed + verified** (qiskit turned out to be reachable through the WL Python bridge; A44). The doc's original cell was broken twice over: positional `QiskitTarget` matches no `QuantumQASM` rule, and the spec lacked the required `"BasisGates"`. Fixed to the verified form.
- [ ] Optional: run `IBMJobSubmit` where an IBM token exists (the only remaining unexecuted cells, with the device-side chart).
- [x] Claim 5 multiway graph **dropped** (user: "not useful"); the view section is now Bloch / diagram+matrix / tensor network / amplitudes chart (A35, A36).
- [x] Claim 6 **restructured** to a single throughline with one idea per section; picks 1 (invariant + coherence), 4 (Bell basis), 5 (Schmidt) folded in (A37). The literal single-vector basis change is now the S1 headline ($\|+\rangle\to\{1,0\}$ in $X$), so the Dicke section stands alone as "eigenbasis of a symmetry."
- [ ] Optional (deferred): the symmetric-subspace / collective-spin *basis object* for a literal Dicke basis-change cell (QF has no built-in 4-qubit collective-spin basis); current S3 uses $\mathrm{Var}\,S_z=0$, which is sufficient.
- [x] **Regenerate `QF-Showcase.nb`** from the final md. **Re-done (A81)** after the A77-A80 edits (POVM circuit, LZ survival plot, Claim 5 rewrite, Husimi side-by-side, Exp fix, QuantumEvolve reword): 93 Input cells (= 93 md code cells), 1 Title / 8 Section / 35 Subsection, 504 InlineFormula / 3 DisplayFormula, 0 TeX leaks; **91 `Ket`/`Bra`/`BraKet` TemplateBoxes, 0 broken-bar boxes**; sampled multi-line cells (Exp, Husimi) round-trip faithfully to source. (Earlier builds: A73 90-cell, A76 89-cell.)

## 5. Verified example map (current showcase)

**All seven claims are cut by their own natural axis (audience labels dropped everywhere, A10 + A12). The doc is text + code only; plots are present as generating code, not embedded images (A19).**

| Claim | Axis | Examples (each kernel-verified unless noted) |
|---|---|---|
| 1 Symbolic-first | **object type**, building to a hard capstone | symbolic `QuantumState` (Bloch) -> **time-dependent** $H(t)$ rotating field, $[H(t_1),H(t_2)]\ne0$, propagator $U(t)$ + Rabi formula -> **Landau-Zener** $U(t)$ in `ParabolicCylinderD` + three-regime survival plot (A78) -> **QAOA** $K_4$ MaxCut, **graph-driven, no hardcoding** (`Hc`/circuit from `EdgeList[g]`; landscape via the **bra-ket-as-one-object** idiom `(oneLayer /* Hc /* oneLayer["Dagger"])[]["Scalar"]` = $\langle\psi\|H_C\|\psi\rangle$; landscape `Plot3D` surface, A65; p=1 max $\approx3.70$ cubic root $<$ true cut $4$ = honest "single layer falls short", A83) -> **capstone: 2-qubit Heisenberg quench, closed-form $S(t)$** |
| 2 Full theory | **object category** (+ the model's two generalities) | open-system channel, **symbolic** purity loss $2\gamma(1-\gamma)\sin^4\tfrac\theta2$ (A32) -> SIC-POVM, **symbolic Born rule** over `["POVMElements"]`: $p_i = \tfrac14(1+\hat r\cdot\hat n_i)$, north-pole collapse (A62) + **POVM-in-circuit**: SIC on qubit 1 of an entangling circuit (`QuantumMeasurementOperator["TetrahedronSICPOVM",{1}]`), `ProbabilitiesPlot` of the mixed entangled marginal ($\|\hat r\|\approx0.87$, probs pulled toward uniform; A77) -> **coherent state symbolic** ($\hat a\|\alpha\rangle = \alpha\|\alpha\rangle$ zeros; $e^{-iHt}\|\alpha\rangle = e^{-i\omega t/2}\|\alpha e^{-i\omega t}\rangle$ -> `True`, $\alpha,\omega,t$ symbolic; A71) -> optics $g^{(2)}$ ($0/1/2$) -> **cat-state Wigner + Husimi side by side** (`CatState[][2, 0]`: Wigner central fringe $+0.318\approx1/\pi$, trough $-0.236$, lobes $\approx1/2\pi$ at $\pm2\sqrt2$, A66; Husimi `HusimiQRepresentation` non-negative everywhere, $\approx0.012$ at origin, side-by-side `Row[{Plot3D,Plot3D}]`/`SunsetColors` contrast = nonclassicality witness; Husimi build widened to $p\in[-3,3]$ to match the taller plot, A80/A88) -> *(discrete-Wigner magic, KCBS contextuality, single-qudit Grover all MOVED out to [QF-Showcase-Qudit-Extras.md](QF-Showcase-Qudit-Extras.md) to refocus Claim 2 on the object model; still verified there; A87 built them, A89 de-hardcoded KCBS, A90 relocated them)* -> **basis on every object** (mechanism para; $\|+\rangle$ -> $\|x_+\rangle$; **spin-1 three-basis invariant** $\langle J_x\rangle = 1$, A47/A59) -> **any finite dimension** (symbolic spin-1 ZFS -> $\operatorname{diag}(0, 2d-e, 2d+e)$; $N{=}5$ ring: circulant `MatrixForm` cell + Fourier-diagonal cell -> $-2\cos(2\pi k/5)$; A47/A59/A67) -> **master equation solved symbolically** (all-states $\rho_0$, all rates -> relaxation law; $T_2 \le 2T_1$ via `Reduce` = True; A57) -> **state estimation** (tomography: X/Y/Z @ 2000 shots -> fidelity $\approx0.99998$; A47). [Stabilizer section moved to Claim 4, A64] |
| 3 Feedforward | **protocol** | record-as-wire seed (GHZ+QFT, three measurements, `"CNOT" -> {-1, 2}`; diagram) -> **deferred-measurement teleportation** (corrections `"CNOT" -> {-1, 3}` and `"CZ" -> {0, 3}`; Bloch difference $\{0,0,0\}$ symbolically for $\psi(\theta,\phi)$) -> **repetition code, coherent $R_X(2\theta)$ error**: projector-list syndromes $(I\pm ZZ)/2$, mixed-polarity `"C"[X, ones, zeros]` decode, syndrome weights $\{\cos^2\theta, 0, 0, \sin^2\theta\}$ (digitization), codeword fidelity **exactly 1** -> **un-measurement** via `m["SuperOperator"]["Dagger"]` (input recovered exactly, $\{0,0\}$ residual symbolic); opener now flags the **lab counterpart** (weak-measurement "uncollapsing" on superconducting qubits, A98); explicit $2^{n+k}$ cost paragraph (A64) |
| 4 Engines | **computational engine** | **TN default, steered**: **Grover search** (named `"GroverPhase"` iteration $G=D\,O$, stacked $k{=}\lfloor\tfrac\pi4\sqrt N\rceil{=}12$ on $|+\rangle^{\otimes8}$) greedy-path apply -> finds the marked input $200{=}11001000$ at success $0.9999$ on the analytic peak $\sin^2((2k{+}1)\arcsin\sqrt{1/N})$ + `ContractionTree` of the greedy path; honest framing (full-width diffusion, TN does NOT beat dense at n=8; the named Grover is the ITERATION operator, run via stacking on $|+..+\rangle$, A94, replaced QFT[12] A54/A75) -> **stabilizer engine**: **5-qubit linear cluster** `cluster5` (H+CZ) shown one statement per cell `cluster5["Diagram"]` -> `stab5 = cluster5[Method -> "Stabilizer"]` -> `stab5["PauliForm"]` $\{XZIII,ZXZII,IZXZI,IIZXZ,IIIZX\}=K_v$ (engine chosen at apply time, $2^5$ vector avoided); expectations $\langle K_1\rangle=\langle K_3\rangle=1$, $\langle XXIII\rangle=0$; $n{=}1000$, 19,998 gates via `ApplyCircuit` **0.06 s** -> Fattal entropy = 496 ebits (A56/A75/A95) -> **interconversion**: **3-qubit linear cluster** `cluster3`/`stab3`, one statement per cell: `cluster3["Diagram"]` -> `stab3 = cluster3[Method -> "Stabilizer"]` -> `QuantumState[stab3]` $=\tfrac1{\sqrt8}\sum_x(-1)^{x_1x_2+x_2x_3}|x\rangle$ (signs on $011,110$) -> `QuantumState[stab3]["StateVector"] === cluster3[]["StateVector"]` `True` (prose names it `SameQ`, global phase incl.) -> `stab3["Circuit"]["Diagram"]` 5-gate synthesis (A64/A75/A96/A99) |
| 5 Ordinary WL expression | **native WL function operates ON the object** (rewritten A79) | arithmetic `(\|0>+\|1>)/Sqrt2 == \|+>` -> **`MatrixExp`** generator->propagator $e^{-iZt}$ -> operator algebra `aX+bZ` + `PauliDecompose` round-trip + `Commutator[X,Y]==2I Z` -> **`D` operator-calculus H-recovery** $H=i\dot U U^\dagger$ (moved from Claim 1, counterdiabatic) -> state `D` harmonic identity -> solvers `Maximize[<X+Z>]`=$\sqrt2@\pi/4$ + `Reduce` Werner threshold $p<\tfrac12$. Honesty clause: overload set finite (`Eigenvalues`/`Det` via `["Matrix"]`); `Integrate`/`Series`/`NProbability`-distribution interop deliberately excluded (wrong/unevaluated). [old property-dispatch + boring H2 Association + numeric Plot cut, A79] |
| 6 Visualization | **object kind, one viz each** | Bloch sphere (`["BlochPlot"]`) -> **AmplitudesChart of QFT(cat $\|000\rangle+\|101\rangle$)** (interference comb + phase ramp, null at $\|100\rangle$; A75) -> **Bernstein-Vazirani measurement histogram** (`["ProbabilitiesPlot"]`, deterministic spike at hidden $1011$; A75) -> **5-ring cluster stabilizer tableau** (`PauliStabilizer[Graph[C5]]["TableauForm"]`, $Z$ block = adjacency matrix; A59/A75) -> **QPE[3]** circuit `["Diagram"]` + `["TensorNetworkGraph"]` (21 vertices) + TN `["Hypergraph"]` (21 edges) (A59/A75). Full audited catalog (PieChart/SectorChart, adiabatic plots, multiway/causal views, Wigner surfaces) in the intro prose (A55) |
| 7 Interop | **export target** (current API) | `QuantumQASM` export of **Toffoli** (3.0 native, emits Clifford+T decomposition; A33/A75) -> import foreign OpenQASM = **CHSH circuit** with symbolic `Pi` angles (QF reads `U(Pi/2, 0, Pi)`; qiskit rejects capital `Pi` as undefined, A97), `Probability // FullSimplify` win $=\tfrac{2+\sqrt2}{4}=\cos^2\tfrac\pi8\approx0.8536$ Tsirelson, **exact closed form** (native, verified) -> `QiskitTarget` transpile of **QFT[3]** (**executed + verified** via Python bridge: `"Target" -> target` option form, spec needs `"BasisGates"`; A44/A75) -> `IBMJobSubmit`/`IBMJob` on a **measured** circuit (`{"GHZ"[3], {1,2,3}}`), `wl = qc[]` vs `qpu = job["Refresh"][]`, one paired exact-vs-QPU `BarChart` (doc-page idiom; A52; **executed live on `ibm_fez`, 4096 shots, A58**). Symbolic-angle export `Failure` caveat folded into export note, A34 |
(Numbering note, four renumberings deep: the original Claim 5 "Many views" was dropped (A38); the basis claim (originally 6, then 5) was dissolved into Claims 2 and 3 (A47), leaving four claims; A55 added **Claim 4 "Every object draws itself"** and renumbered interop 4 -> 5, giving five claims; A64 inserted **Claim 3 "Measurement records are quantum wires"** and **Claim 4 "Three computational engines"** and renumbered WL-expression 3 -> 5, visualization 4 -> 6, interop 5 -> 7, so the doc now has **seven** claims. A12-A37 use the original numbering (5 = views, 6 = basis); A38-A46 the interim (5 = basis); A47-A54 the four-claim numbering (4 = interop); A55-A63 the five-claim numbering (4 = visualization, 5 = interop); A64+ the current seven-claim numbering.)

Boundary kept explicit: **Claim 7 = leaves/enters QF via another tool (and hardware)**, and it still closes the document.

## 6. Process lessons (the reusable essence)

These generalize beyond this task.

1. **Pick the organizing axis from the claim's mechanism, not a fixed template.** A claim about a property that *pervades the object model* (Claim 1, "symbolic-first") is best cut by **object type**; a claim about *who benefits* may be cut by **audience**; a claim about *coverage* may be cut by **physical regime**. Forcing an audience grid onto Claim 1 added noise (A10). Decide the axis first, then fill it.

2. **Keep claims on orthogonal axes, and watch for cross-contamination.** Two claims should differentiate along *different* dimensions (see the Claim 1 vs Claim 2 note below). If a Claim-2 example's wow-factor is actually its symbolic closed form, it is silently doing Claim 1's job. Make each example carry *its own* claim's punch.

3. **A rejected tool can return in a smaller role.** `QuantumEvolve` was wrong as the *headline* of "symbolic-first" (A1) but correct as *one example* showing operator -> dynamics (A11). The fix to a bad example is often re-scoping, not deletion.

4. **Tie examples together for a through-line.** The evolution frequency $\sqrt{J^2+h^2}$ equals the spectrum gap from the previous example; the VQE minimum equals that same eigenvalue. Internal echoes make a showcase read as one argument, not a list.

5. **Self-reported "done" is unreliable; re-read the artifact against the literal ask.** The symbolic-state gap (A9) survived a "done" claim and was only caught on a forced re-read. Verify the deliverable, not your memory of it.

6. **Lead with what the tool does, not what others lack.** The first draft leaned on "Qiskit can't / no numeric platform can match" (A18). A feature tour reads better and ages better when it states the capability positively; competitors appear only where they are genuinely relevant (interop). Keep contrast-test reasoning in *this* design doc, out of the showcase.

7. **For any symbol newer than the audit anchor, read the source; memory is a pointer, not the API.** The whole interop surface (`QuantumQASM`/`IBMJob`/...) post-dates the original anchor and `IBMQuantum.m` drifted +151 lines even past the re-sync (A22). Verifying in `*.m` + kernel turned an "illustrative, not executed" example into a runnable one and caught the legacy/deprecated calls.

8. **For an educational section, switch register to learning-by-computing.** Claim 6 reads as concept -> bridge sentence -> *one* short cell -> interpretation, one computation per cell (A24). Packing several computations into one cell (the first Claim 6 draft) hides the result the reader is supposed to see.

9. **A "unique feature" section needs non-trivial, literature-grounded examples.** The Bell state and `XX+YY+ZZ` were too cute (A24). Reach for the computations researchers actually run in that regime (H$_2$ Pauli grouping, MUB tomography, Wigner negativity, SIC/QBism) and cite them.

10. **Discuss outcomes as prose with inline math, never as `$$label = formula$$` pseudo-output blocks** (A46). The template is the Claim 1 amplitudes line: "The amplitudes return exactly as $\{\cos\alpha,\ e^{i\beta}\sin\alpha\}$, symbols intact", i.e. a sentence that states the closed form inline and immediately glosses it. Display math is reserved for genuinely two-dimensional results (a matrix), and even then it must be bridged before and interpreted after.

### Claim 1 vs Claim 2: the axes they differentiate on
- **Claim 1 ("symbolic-first") differentiates on the *exactness / computation-mode* axis:** symbolic closed forms vs floating-point. The contrast-test win is *the form of the answer* ($\cos(t\sqrt{J^2+h^2})$, not `0.7071...`). It can apply to *any* object type.
- **Claim 2 ("full theory") differentiates on the *scope / ontology* axis:** the existence of object categories (channels, POVMs, open-system/Lindblad, second-quantized optics, tomography) that a gate-only framework does not have at all. The contrast-test win is *that the object exists*, independent of whether its output is symbolic.
- **Risk:** they overlap when a Claim-2 example leans on a symbolic result (e.g. the amplitude-damping coherence $\sqrt{1-\gamma}/2$ is *also* a Claim-1 flex). To keep them distinct, Claim 2 examples should foreground the *object category* and ideally include a **numeric** one (the $g^{(2)}=0/1/2$ photon statistics is good precisely because it is numeric, isolating "scope" from "symbolic").
